# Система автоматического тестирования UI с помощью компьютерного зрения

Система для автоматического тестирования пользовательского интерфейса с использованием YOLOv8, OpenCV и машинного обучения.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Установите Python 3.10+ (рекомендуется 3.12)
# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка базы данных

#### Вариант A: SQLite (для разработки)

```bash
cd autotest_ui
python manage.py migrate
python manage.py createsuperuser
```

#### Вариант B: PostgreSQL (для продакшена)

1. Установите PostgreSQL и создайте базу данных:
```sql
CREATE DATABASE autotest_ui;
CREATE USER autotest_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE autotest_ui TO autotest_user;
```

2. Создайте файл `.env` в корне проекта:
```env
DB_NAME=autotest_ui
DB_USER=autotest_user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
```

3. Обновите `settings.py` для использования PostgreSQL (см. `database/init_db.sql`)

4. Примените миграции:
```bash
cd autotest_ui
python manage.py migrate
python manage.py createsuperuser
```

### 3. Настройка Redis (для Celery)

#### Вариант A: Docker
```bash
docker run -d -p 6379:6379 redis:7-alpine
```

#### Вариант B: Локальная установка
- Установите Redis и запустите: `redis-server`

### 4. Настройка переменных окружения

Создайте файл `.env` в корне проекта (если еще не создан):

```env
# База данных (если используете PostgreSQL)
DB_NAME=autotest_ui
DB_USER=autotest_user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432

# Django
DJANGO_SECRET_KEY=your-secret-key-change-me
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Celery (Redis)
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Режим выполнения задач (1 = синхронно, 0 = через Celery)
TASKS_FORCE_SYNC=1

# YOLOv8
USE_YOLO_DETECTION=1
YOLO_CONF_THRESHOLD=0.25

# Jira (опционально)
JIRA_URL=https://your-company.atlassian.net
JIRA_USERNAME=your-email@example.com
JIRA_API_TOKEN=your-api-token
JIRA_PROJECT_KEY=TEST
```

### 5. Запуск проекта

#### Windows (PowerShell):

```powershell
# Терминал 1: Django сервер
cd autotest_ui
python manage.py runserver

# Терминал 2: Celery worker (если TASKS_FORCE_SYNC=0)
celery -A autotest_ui worker -l info --pool=solo

# Терминал 3: Flower - мониторинг Celery (опционально)
.\start_flower.bat
```

#### Linux/Mac:

```bash
# Терминал 1: Django сервер
cd autotest_ui
python manage.py runserver

# Терминал 2: Celery worker (если TASKS_FORCE_SYNC=0)
celery -A autotest_ui worker -l info

# Терминал 3: Flower - мониторинг Celery (опционально)
./start_flower.sh
```

### 6. Доступ к системе

#### Основные интерфейсы:
- **Веб-интерфейс:** http://localhost:8000/
- **API:** http://localhost:8000/api/
- **Админка:** http://localhost:8000/admin/

#### Мониторинг и аналитика:
- **📊 Аналитика системы:** http://localhost:8000/admin/analytics/
- **🚀 CI/CD Dashboard:** http://localhost:8000/cicd/
- **🌸 Flower (Celery мониторинг):** http://localhost:5555 (логин: `admin`, пароль: `admin123`)

Войдите с учетными данными суперпользователя, созданного на шаге 2.

---

## 📊 Новые возможности

### 🌸 Flower - Мониторинг Celery

Flower - это веб-интерфейс для мониторинга и управления Celery задачами:

- ✅ **Просмотр активных задач** в реальном времени
- 📊 **История выполненных задач** с детальной информацией
- ⏱️ **Время выполнения** и статистика
- ❌ **Traceback ошибок** для неудачных задач
- 💻 **Мониторинг воркеров** (нагрузка CPU/RAM)
- 🔄 **Управление задачами** (отмена, перезапуск)

📖 **Подробнее:** [FLOWER_MONITORING.md](FLOWER_MONITORING.md)

### 🚀 CI/CD Dashboard

Веб-интерфейс для просмотра результатов CI/CD сборок:

- 📊 **Дашборд** со всеми сборками
- 🔍 **Детали сборки** с метриками
- ✅ **Success Rate** по сборкам
- 🐛 **Дефекты** по сборкам
- 📊 **Покрытие UI-элементов**

📖 **Подробнее:** [CI_CD_USAGE.md](CI_CD_USAGE.md)

---

## 📚 Документация

- [YOLOV8_INTEGRATION.md](YOLOV8_INTEGRATION.md) - Интеграция YOLOv8
- [SECURITY_AND_DELETE_IMPROVEMENTS.md](SECURITY_AND_DELETE_IMPROVEMENTS.md) - Безопасность и удаление
- [TZ_ANALYSIS_CURRENT.md](TZ_ANALYSIS_CURRENT.md) - Анализ соответствия ТЗ
- [USER_GUIDE.md](USER_GUIDE.md) - Руководство пользователя
- [CI_CD_USAGE.md](CI_CD_USAGE.md) - **NEW!** Интеграция с CI/CD
- [FLOWER_MONITORING.md](FLOWER_MONITORING.md) - **NEW!** Мониторинг Celery через Flower

---

## 💻 Основные функции

### 1. Автогенерация тестов из скриншотов
- Загрузите эталонный скриншот макета
- Система автоматически обнаружит UI элементы с помощью YOLOv8
- Классифицирует элементы (button, input, label, image, link)
- Извлечет текст с помощью OCR

### 2. Сравнение эталонного и реального UI
- Загрузите скриншот реального интерфейса
- Система сравнит с эталоном используя SSIM
- Определит различия и создаст дефекты
- Вычислит метрики покрытия

### 3. Интеграции
- **CI/CD:** Webhooks для GitHub, GitLab, Jenkins
- **Jira:** Автоматическое создание задач при обнаружении дефектов
- **Selenium:** Модуль для автоматического захвата скриншотов

---

## 🛠️ Технологии

- **Django 5.2** - веб-фреймворк
- **DRF** - REST API
- **Celery + Redis** - фоновые задачи
- **Flower** - мониторинг Celery
- **YOLOv8** - детектирование UI элементов
- **OpenCV** - обработка изображений
- **OCR (Tesseract)** - распознавание текста
- **ML (Random Forest)** - классификация элементов

---

## 📚 Структура проекта

```
autotest_ui/
├── autotest_ui/          # Настройки Django
│   ├── settings.py
│   ├── urls.py
│   └── celery.py
├── testsystem/           # Основное приложение
│   ├── models.py         # Модели БД
│   ├── views.py          # API endpoints
│   ├── views_web.py      # Веб-интерфейс
│   ├── views_cicd.py     # CI/CD dashboard
│   ├── tasks.py          # Celery задачи
│   ├── cv_utils.py       # Утилиты CV
│   ├── yolo_detector.py  # YOLOv8 интеграция
│   ├── ml_classifier.py  # ML классификатор
│   ├── jira_integration.py
│   └── ci_integration/   # CI/CD интеграция
├── selenium_integration/ # Selenium модуль
└── media/                # Загруженные файлы
```

---

## 🔧 Дополнительные настройки

### OCR (Tesseract)

Для распознавания текста установите Tesseract OCR:
- **Windows:** https://github.com/UB-Mannheim/tesseract/wiki
- **Linux:** `sudo apt-get install tesseract-ocr`
- **Mac:** `brew install tesseract`

### YOLOv8

Модель `yolov8s.pt` уже находится в `autotest_ui/testsystem/ml_models/`

---

## 🔒 Безопасность

- Все операции записи требуют авторизации
- Удаление доступно только владельцам или суперпользователям
- CI/CD webhooks проверяют подпись
- Файлы автоматически удаляются при удалении объектов

---

## 🐛 Решение проблем

### Проблема: Celery не запускается
**Решение:** Установите `TASKS_FORCE_SYNC=1` в `.env` для синхронного выполнения задач

### Проблема: YOLOv8 не работает
**Решение:** Убедитесь, что `ultralytics` установлен: `pip install ultralytics`

### Проблема: OCR не работает
**Решение:** Установите Tesseract OCR и добавьте в PATH

### Проблема: Миграции не применяются
**Решение:** Убедитесь, что база данных создана и настройки в `.env` правильные

### Проблема: Flower не показывает задачи
**Решение:** Проверьте, что Celery worker запущен и Redis работает

---

## 📝 Лицензия

Проект для курсовой работы.

---

## 👤 Автор

Система автоматического тестирования UI с компьютерным зрением.
