# 📖 Руководство пользователя: Система автоматического тестирования UI

## 🎯 Что это за приложение?

Это система для автоматического тестирования пользовательского интерфейса веб-приложений с помощью компьютерного зрения. Она позволяет:

- ✅ Автоматически находить элементы UI на скриншотах
- ✅ Сравнивать эталонный дизайн с реальным состоянием
- ✅ Обнаруживать дефекты и отклонения
- ✅ Интегрироваться с CI/CD системами
- ✅ Автоматически создавать задачи в Jira при обнаружении дефектов
- ✅ Использовать Selenium для автоматического захвата скриншотов

---

## 🚀 Как это работает: Основной процесс

### Шаг 1: Создание тест-кейса

**Что делает пользователь:**
1. Загружает эталонный скриншот (макет/дизайн) через API
2. Система сохраняет его как `TestCase`

**API запрос:**
```bash
POST /api/testcases/
Content-Type: multipart/form-data

{
  "title": "Главная страница",
  "description": "Проверка главной страницы сайта",
  "reference_screenshot": <файл изображения>
}
```

**Что происходит внутри:**
- Скриншот сохраняется в `media/references/`
- Тест-кейс получает статус `new`

---

### Шаг 2: Анализ эталонного скриншота

**Что делает пользователь:**
Запускает анализ макета для автоматического поиска элементов UI

**API запрос:**
```bash
POST /api/testcases/{id}/analyze/
```

**Что происходит внутри:**
1. Система использует **компьютерное зрение** (OpenCV) для поиска элементов
2. Применяет несколько методов детектирования:
   - Адаптивная бинаризация
   - Поиск контуров
   - Canny edges
3. **Классифицирует** каждый найденный элемент:
   - `button` - кнопки
   - `input` - поля ввода
   - `label` - текстовые метки
   - `image` - изображения
   - `link` - ссылки
   - `unknown` - неизвестные элементы
4. **Извлекает текст** с помощью OCR (если установлен pytesseract)
5. Сохраняет каждый элемент с:
   - Координатами (bbox в относительных координатах 0-1)
   - Типом элемента
   - Извлеченным текстом
   - Уровнем уверенности (confidence)

**Результат:**
- Тест-кейс получает статус `analyzed`
- Создаются объекты `UIElement` для каждого найденного элемента
- Можно посмотреть найденные элементы: `GET /api/testcases/{id}/elements/`

---

### Шаг 3: Создание прогона теста

**Что делает пользователь:**
Загружает скриншот текущего состояния приложения для сравнения

**API запрос:**
```bash
POST /api/runs/
Content-Type: multipart/form-data

{
  "testcase": <id тест-кейса>,
  "actual_screenshot": <файл изображения>,
  "ci_job_id": "job-123"  # опционально, для CI/CD
}
```

**Что происходит внутри:**
- Скриншот сохраняется в `media/runs/`
- Создается объект `Run` со статусом `processing`

---

### Шаг 4: Сравнение эталона с реальностью

**Что делает пользователь:**
Запускает сравнение эталонного и фактического скриншотов

**API запрос:**
```bash
POST /api/runs/{id}/compare/
```

**Что происходит внутри:**
1. Система загружает оба скриншота
2. Приводит их к одному размеру
3. Вычисляет разницу с помощью `cv2.absdiff`
4. Рассчитывает метрики:
   - `diff_score` - общая разница (0-1)
   - `mismatch_ratio` - доля несовпадающих пикселей
   - `coverage_percent` - процент покрытия (сколько элементов совпало)
5. Создает `CoverageMetric` с детальной статистикой:
   - Общее количество элементов
   - Количество совпавших элементов
   - Количество несовпавших элементов
   - Процент покрытия
6. Если найдены отклонения (`mismatch_ratio > 0.1`):
   - Создает объект `Defect` (дефект)
   - Определяет серьезность: `minor`, `major`, `critical`
   - **Автоматически создает задачу в Jira** (если настроено)
7. Обновляет статус прогона на `finished`
8. **Отправляет callback в CI/CD систему** (если указан `ci_job_id`)

**Результат:**
- Прогон получает статус `finished`
- Можно посмотреть результаты: `GET /api/runs/{id}/`
- Можно посмотреть дефекты: `GET /api/runs/{id}/defects/`
- Можно посмотреть метрики: `GET /api/runs/{id}/coverage_metric/`

---

## 📊 Что может делать пользователь через API

### 1. Управление тест-кейсами

```bash
# Создать тест-кейс
POST /api/testcases/
{
  "title": "Название",
  "description": "Описание",
  "reference_screenshot": <файл>
}

# Получить список тест-кейсов
GET /api/testcases/

# Получить конкретный тест-кейс
GET /api/testcases/{id}/

# Обновить тест-кейс
PUT /api/testcases/{id}/

# Удалить тест-кейс
DELETE /api/testcases/{id}/

# Запустить анализ макета
POST /api/testcases/{id}/analyze/

# Получить найденные элементы
GET /api/testcases/{id}/elements/
```

### 2. Управление прогонами

```bash
# Создать прогон
POST /api/runs/
{
  "testcase": <id>,
  "actual_screenshot": <файл>,
  "ci_job_id": "job-123"  # опционально
}

# Получить список прогонов
GET /api/runs/

# Фильтрация по CI job ID
GET /api/runs/?ci_job_id=job-123

# Фильтрация по статусу
GET /api/runs/?status=finished

# Получить конкретный прогон
GET /api/runs/{id}/

# Запустить сравнение
POST /api/runs/{id}/compare/

# Получить статус для CI/CD
GET /api/runs/ci-status/?ci_job_id=job-123

# Детальный статус прогона
GET /api/runs/{id}/ci-status-detail/
```

### 3. Просмотр элементов UI

```bash
# Получить все элементы
GET /api/elements/

# Получить конкретный элемент
GET /api/elements/{id}/

# Фильтрация по тест-кейсу (через query параметры)
GET /api/elements/?testcase=<id>
```

### 4. Просмотр дефектов

```bash
# Получить все дефекты
GET /api/defects/

# Получить конкретный дефект
GET /api/defects/{id}/

# Фильтрация по тест-кейсу
GET /api/defects/?testcase=<id>

# Фильтрация по прогону
GET /api/defects/?run=<id>
```

### 5. Просмотр метрик покрытия

```bash
# Получить все метрики
GET /api/coverage/

# Получить конкретную метрику
GET /api/coverage/{id}/
```

---

## 🔗 Интеграции

### 1. Интеграция с CI/CD

**Автоматический режим (Webhook):**

CI/CD система отправляет webhook → система создает прогон → выполняет сравнение → отправляет результаты обратно

**Webhook эндпоинты:**
- `POST /api/webhooks/ci/github/` - для GitHub Actions
- `POST /api/webhooks/ci/gitlab/` - для GitLab CI
- `POST /api/webhooks/ci/jenkins/` - для Jenkins
- `POST /api/webhooks/ci/generic/` - универсальный

**Ручной режим (API):**
```bash
# 1. Создать прогон с ci_job_id
POST /api/runs/
{
  "testcase": 1,
  "actual_screenshot": <файл>,
  "ci_job_id": "github-actions-123"
}

# 2. Запустить сравнение
POST /api/runs/{id}/compare/

# 3. Получить результаты
GET /api/runs/ci-status/?ci_job_id=github-actions-123
```

### 2. Интеграция с Jira

**Автоматическое создание задач:**

При обнаружении дефекта система автоматически:
1. Создает задачу в Jira
2. Сохраняет ключ задачи в поле `task_tracker_issue` прогона
3. Сохраняет ключ задачи в метаданных дефекта

**Настройка:**
```env
JIRA_URL=https://your-company.atlassian.net
JIRA_USERNAME=your-email@example.com
JIRA_API_TOKEN=your-api-token
JIRA_PROJECT_KEY=TEST
```

**Что создается в Jira:**
- Тип задачи: `Bug` (для critical/major) или `Task` (для minor)
- Название: "UI Defect: {название тест-кейса}"
- Описание включает:
  - Описание дефекта
  - ID тест-кейса и прогона
  - Серьезность
  - Метрики покрытия
  - Ссылку на скриншот (если есть)

### 3. Интеграция с Selenium

**Использование готового модуля:**

```python
from selenium_integration.screenshot_capture import ScreenshotCapture

# Создаем экземпляр
capture = ScreenshotCapture(
    api_base_url="http://localhost:8000",
    headless=True
)

# Захватываем скриншот, запускаем сравнение и ждем результатов
result = capture.capture_compare_and_wait(
    url="https://example.com",
    testcase_id=1,
    wait_for_element={'by': 'TAG_NAME', 'value': 'body'},
    ci_job_id="selenium-test-123"
)

# Результат содержит:
# - status: 'finished' или 'failed'
# - coverage: процент покрытия
# - reference_diff_score: оценка различий
# - defects: список дефектов
```

**Пример использования:**
См. `autotest_ui/selenium_integration/examples/basic_test.py`

---

## 🎨 Примеры использования

### Пример 1: Базовый workflow

```bash
# 1. Создать тест-кейс
curl -X POST http://localhost:8000/api/testcases/ \
  -F "title=Главная страница" \
  -F "description=Проверка главной страницы" \
  -F "reference_screenshot=@screenshot.png"

# Ответ: {"id": 1, "title": "Главная страница", ...}

# 2. Запустить анализ макета
curl -X POST http://localhost:8000/api/testcases/1/analyze/

# Ответ: {"task_id": "abc123", "status": "queued"}

# 3. Подождать завершения анализа (проверить статус через Celery)

# 4. Посмотреть найденные элементы
curl http://localhost:8000/api/testcases/1/elements/

# 5. Создать прогон с текущим скриншотом
curl -X POST http://localhost:8000/api/runs/ \
  -F "testcase=1" \
  -F "actual_screenshot=@current_screenshot.png"

# Ответ: {"id": 1, "testcase": 1, "status": "processing", ...}

# 6. Запустить сравнение
curl -X POST http://localhost:8000/api/runs/1/compare/

# 7. Получить результаты
curl http://localhost:8000/api/runs/1/

# Ответ содержит:
# {
#   "id": 1,
#   "status": "finished",
#   "coverage": 85.5,
#   "reference_diff_score": 0.12,
#   "coverage_metric": {
#     "total_elements": 20,
#     "matched_elements": 17,
#     "mismatched_elements": 3,
#     "coverage_percent": 85.0
#   },
#   "defects": [
#     {
#       "id": 1,
#       "severity": "major",
#       "description": "UI deviation exceeds threshold",
#       ...
#     }
#   ]
# }
```

### Пример 2: Использование с CI/CD

```bash
# В CI/CD pipeline (GitHub Actions, GitLab CI, Jenkins):

# 1. Сделать скриншот текущего состояния
# (через Selenium или другой инструмент)

# 2. Создать прогон с ci_job_id
curl -X POST http://your-api.com/api/runs/ \
  -F "testcase=1" \
  -F "actual_screenshot=@screenshot.png" \
  -F "ci_job_id=$CI_JOB_ID"

# 3. Запустить сравнение
curl -X POST http://your-api.com/api/runs/{run_id}/compare/

# 4. Подождать завершения (или использовать webhook callback)

# 5. Получить результаты
curl http://your-api.com/api/runs/ci-status/?ci_job_id=$CI_JOB_ID

# 6. Если есть дефекты - провалить pipeline
if [ "$(jq '.summary.has_defects' results.json)" == "true" ]; then
  exit 1
fi
```

### Пример 3: Использование Selenium модуля

```python
from selenium_integration.screenshot_capture import ScreenshotCapture

# Настройка
capture = ScreenshotCapture(
    api_base_url="http://localhost:8000",
    headless=True
)

# Тестирование страницы
result = capture.capture_compare_and_wait(
    url="https://example.com",
    testcase_id=1,
    wait_for_element={'by': 'ID', 'value': 'main-content'},
    timeout=300
)

# Проверка результатов
if result and result.get('status') == 'finished':
    coverage = result.get('coverage', 0)
    if coverage < 90:
        print(f"Покрытие ниже 90%: {coverage}%")
    
    defects = result.get('defects', [])
    if defects:
        print(f"Найдено дефектов: {len(defects)}")
        for defect in defects:
            print(f"  - {defect['severity']}: {defect['description']}")
```

---

## 📈 Метрики и отчеты

### Что можно отслеживать:

1. **Покрытие тестами:**
   - Процент совпавших элементов
   - Количество найденных элементов
   - Количество несовпавших элементов

2. **Качество UI:**
   - Общая оценка различий (diff_score)
   - Количество дефектов по серьезности
   - История изменений (через прогоны)

3. **Интеграции:**
   - Статусы CI/CD джоб
   - Созданные задачи в Jira
   - История автоматических тестов

---

## 🔧 Настройка

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# База данных
DB_NAME=autotest_ui
DB_USER=autotest_user
DB_PASSWORD=autotest_password
DB_HOST=localhost
DB_PORT=5432

# Django
DJANGO_SECRET_KEY=your-secret-key
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Celery/Redis
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# Jira (опционально)
JIRA_URL=https://your-company.atlassian.net
JIRA_USERNAME=your-email@example.com
JIRA_API_TOKEN=your-api-token
JIRA_PROJECT_KEY=TEST

# CI/CD (опционально)
GITHUB_WEBHOOK_SECRET=your-secret
GITLAB_WEBHOOK_TOKEN=your-token
JENKINS_WEBHOOK_SECRET=your-secret
CI_CALLBACK_ENABLED=1
CI_API_BASE_URL=http://localhost:8000
```

---

## 🎯 Типичные сценарии использования

### Сценарий 1: Регрессионное тестирование

1. Загружаете эталонный скриншот после успешного релиза
2. Запускаете анализ макета
3. При каждом деплое делаете скриншот текущего состояния
4. Запускаете сравнение
5. Получаете отчет о дефектах

### Сценарий 2: Интеграция в CI/CD

1. Настраиваете webhook в CI/CD системе
2. При каждом коммите автоматически:
   - Делается скриншот
   - Создается прогон
   - Выполняется сравнение
   - Результаты отправляются обратно в CI/CD
3. При обнаружении дефектов pipeline может быть остановлен

### Сценарий 3: Автоматическое тестирование с Selenium

1. Пишете Selenium тесты
2. Используете модуль `selenium_integration` для захвата скриншотов
3. Автоматически загружаете скриншоты в систему
4. Получаете результаты сравнения
5. Дефекты автоматически создаются в Jira

---

## 📚 Дополнительная документация

- **Полное руководство по CI/CD:** `CI_CD_COMPLETE_GUIDE.md`
- **Инструкция по запуску:** `HOW_TO_RUN_AND_TEST.md`
- **Отчет о соответствии ТЗ:** `TZ_COMPLIANCE_REPORT.md`
- **Статус реализации:** `IMPLEMENTATION_STATUS.md`

---

## ❓ Часто задаваемые вопросы

**Q: Нужно ли устанавливать Tesseract для OCR?**  
A: Нет, это опционально. Если не установлен, система просто не будет извлекать текст, но остальной функционал работает.

**Q: Как работает классификация элементов?**  
A: Система использует эвристики на основе формы, размера, контрастности и других характеристик элементов. Это базовая классификация, для более точной можно использовать ML-модели.

**Q: Можно ли использовать без Jira?**  
A: Да, интеграция с Jira опциональна. Если не настроена, дефекты просто сохраняются в базе данных.

**Q: Как работает интеграция с CI/CD?**  
A: CI/CD система отправляет webhook или использует API для создания прогонов. После завершения сравнения система отправляет callback обратно в CI/CD.

**Q: Можно ли использовать для мобильных приложений?**  
A: Да, система работает с любыми скриншотами. Можно делать скриншоты мобильных приложений и сравнивать их.

---

## 🎉 Готово!

Теперь вы знаете, как использовать систему автоматического тестирования UI. Начните с создания первого тест-кейса и запуска анализа!

