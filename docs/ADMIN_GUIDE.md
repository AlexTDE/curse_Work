# 🛡️ Руководство администратора

## 📋 Содержание

1. [Вход в админ-панель](#вход-в-админ-панель)
2. [Создание суперпользователя](#создание-суперпользователя)
3. [Обзор админ-панели](#обзор-админ-панели)
4. [Панель аналитики](#панель-аналитики)
5. [Управление тест-кейсами](#управление-тест-кейсами)
6. [Управление пользователями](#управление-пользователями)
7. [Просмотр логов Celery](#просмотр-логов-celery)
8. [Управление версиями эталонов](#управление-версиями-эталонов)

---

## 🔐 Вход в админ-панель

### Шаг 1: Создание суперпользователя

Если у вас ещё нет учётной записи администратора, создайте её:

```bash
# Перейдите в директорию с проектом
cd autotest_ui

# Создайте суперпользователя
python manage.py createsuperuser
```

Вам будет предложено ввести:

```
Username: admin
Email address: admin@example.com
Password: ********
Password (again): ********
```

**Важно:** Используйте надёжный пароль! Минимум 8 символов, буквы + цифры.

### Шаг 2: Запуск сервера

```bash
# Убедитесь, что Django-сервер запущен
python manage.py runserver

# Или используйте скрипт
./start_server.bat  # Windows
./start_server.sh   # Linux/Mac
```

### Шаг 3: Открытие админ-панели

Откройте браузер и перейдите по адресу:

```
http://localhost:8000/admin/
```

Или, если сервер запущен на другом порту:

```
http://localhost:PORT/admin/
```

### Шаг 4: Авторизация

1. Введите `username`, который вы создали
2. Введите пароль
3. Нажмите **"Log in"**

Вы попадёте на главную страницу админ-панели.

---

## 🏠 Обзор админ-панели

После входа вы увидите главную страницу с разделами:

### Основные разделы

```
📊 СИСТЕМА АВТОТЕСТИРОВАНИЯ UI
├── 🔧 Authentication and Authorization
│   ├── Users (Пользователи)
│   └── Groups (Группы)
│
├── 🧪 TESTSYSTEM
│   ├── Test Cases (Тест-кейсы)
│   ├── Runs (Прогоны)
│   ├── UI Elements (UI элементы)
│   ├── Defects (Дефекты)
│   ├── Coverage Metrics (Метрики покрытия)
│   ├── Test Case Versions (Версии эталонов)
│   └── Reference Update Requests (Запросы на обновление)
│
└── 📈 Analytics (Аналитика) ⭐
```

---

## 📊 Панель аналитики

### Доступ к аналитике

Перейдите по адресу:

```
http://localhost:8000/admin/analytics/
```

Или нажмите ссылку **"Analytics"** в верхнем меню админ-панели.

### Что показывает панель аналитики

#### 1. Общая статистика

```json
{
  "total_testcases": 45,          // Всего тест-кейсов
  "total_runs": 237,              // Всего прогонов
  "total_defects": 89,            // Всего дефектов
  "total_ui_elements": 523,       // Всего UI-элементов
  "avg_coverage": 78.54           // Среднее покрытие (%)
}
```

**Где используется:**
- Оценка общего состояния системы
- Отслеживание роста количества тестов
- Мониторинг качества покрытия

#### 2. Распределение по статусам

**Тест-кейсы:**
- `new` — новые, ещё не проанализированные
- `analyzed` — проанализированные (найдены UI-элементы)
- `ready` — готовые к использованию

**Прогоны:**
- `queued` — в очереди на выполнение
- `processing` — выполняются сейчас
- `finished` — успешно завершены
- `failed` — завершены с ошибкой

**Дефекты по серьёзности:**
- `minor` — незначительные
- `major` — значительные
- `critical` — критичные

#### 3. Динамика дефектов во времени

График показывает количество дефектов по дням/неделям/месяцам:

```json
[
  {
    "period": "2025-12-01",
    "total": 12,
    "critical": 2,
    "major": 5,
    "minor": 5
  },
  {
    "period": "2025-12-02",
    "total": 8,
    "critical": 1,
    "major": 3,
    "minor": 4
  }
]
```

**Применение:**
- Отслеживание трендов качества
- Выявление периодов с повышенным числом дефектов
- Планирование ресурсов на исправление

#### 4. Динамика покрытия

График изменения среднего покрытия UI-элементов:

```json
[
  {
    "period": "2025-12-01",
    "avg_coverage": 75.3,
    "max_coverage": 95.2,
    "min_coverage": 45.1,
    "count": 15
  }
]
```

**Применение:**
- Мониторинг улучшения/ухудшения качества тестов
- Выявление проблемных периодов

#### 5. Статистика по пользователям

Активность каждого пользователя:

```json
[
  {
    "username": "alice",
    "testcases_created": 15,
    "runs_started": 87,
    "versions_created": 3,
    "update_requests": 2
  }
]
```

**Применение:**
- Отслеживание вклада каждого тестировщика
- Выявление наиболее активных участников

#### 6. Производительность тест-кейсов

Топ-10 проблемных тест-кейсов с наибольшим количеством дефектов:

```json
[
  {
    "id": 5,
    "title": "Login Page Test",
    "defect_count": 12,
    "run_count": 45,
    "avg_coverage": 68.3
  }
]
```

**Применение:**
- Приоритизация работы по улучшению тестов
- Выявление нестабильных UI-компонентов

#### 7. Производительность прогонов

Метрики за последние N дней:

```json
{
  "total_runs": 87,
  "finished_runs": 78,
  "failed_runs": 9,
  "success_rate": 89.66,
  "failure_rate": 10.34
}
```

**Применение:**
- Мониторинг стабильности системы
- Выявление проблем с инфраструктурой

#### 8. Статистика версионирования

```json
{
  "total_versions": 34,
  "total_update_requests": 12,
  "pending_requests": 3,
  "approved_requests": 7,
  "rejected_requests": 2,
  "versions_by_reason": [
    {"reason": "design_change", "count": 15},
    {"reason": "bug_fix", "count": 10}
  ]
}
```

**Применение:**
- Отслеживание частоты обновлений эталонов
- Анализ причин изменений

### Фильтрация по периоду

Вы можете изменить период анализа, добавив параметр `days`:

```
http://localhost:8000/admin/analytics/?days=7   # Последние 7 дней
http://localhost:8000/admin/analytics/?days=30  # Последние 30 дней (по умолчанию)
http://localhost:8000/admin/analytics/?days=90  # Последние 90 дней
```

---

## 🧪 Управление тест-кейсами

### Просмотр списка тест-кейсов

1. Перейдите в **TESTSYSTEM → Test Cases**
2. Вы увидите список всех тест-кейсов с:
   - ID
   - Названием
   - Статусом (цветной бейдж)
   - Автором
   - Количеством элементов
   - Количеством прогонов
   - Средним покрытием
   - Датой создания

### Фильтрация

Используйте правую боковую панель для фильтрации:
- По статусу (new/analyzed/ready)
- По дате создания
- По автору

### Поиск

Введите текст в поле поиска для поиска по:
- Названию тест-кейса
- Описанию

### Редактирование тест-кейса

1. Кликните на ID или название тест-кейса
2. Вы увидите детальную страницу с вкладками:
   - **Основная информация**: название, описание, статус, автор
   - **Эталонный скриншот**: загрузка и предпросмотр
   - **Статистика**: количество элементов, прогонов, среднее покрытие
   - **UI Elements** (inline): список найденных UI-элементов

3. Измените нужные поля
4. Нажмите **"Save"** или **"Save and continue editing"**

### Удаление тест-кейсов

⚠️ **Внимание:** Удаление тест-кейса удалит все связанные:
- UI-элементы
- Прогоны
- Дефекты
- Версии эталонов

**Для удаления:**
1. Выберите тест-кейсы (чекбоксы слева)
2. В выпадающем списке **"Action"** выберите **"Delete selected test cases"**
3. Нажмите **"Go"**
4. Подтвердите удаление

---

## 👥 Управление пользователями

### Просмотр пользователей

1. Перейдите в **AUTHENTICATION AND AUTHORIZATION → Users**
2. Вы увидите список всех пользователей

### Создание нового пользователя

1. Нажмите **"Add User"** в правом верхнем углу
2. Заполните:
   - Username (обязательно)
   - Password (обязательно)
3. Нажмите **"Save"**
4. На следующей странице заполните дополнительные данные:
   - First name, Last name
   - Email
   - **Permissions** (права доступа):
     - Staff status — доступ к админ-панели
     - Superuser status — полный доступ ко всем функциям

### Изменение прав пользователя

1. Откройте пользователя для редактирования
2. Прокрутите до раздела **"Permissions"**
3. Установите флажки:
   - ☑️ **Active** — пользователь может входить в систему
   - ☑️ **Staff status** — доступ к админ-панели
   - ☑️ **Superuser status** — полные права администратора

4. Или назначьте конкретные права в **"User permissions"**

---

## 📋 Просмотр логов Celery

### Через командную строку

```bash
# Запустите Celery worker с выводом логов
celery -A autotest_ui worker --loglevel=info
```

Вы увидите логи в реальном времени:

```
[2025-12-05 19:00:15] Task testsystem.tasks.generate_test_from_screenshot[abc123] received
[2025-12-05 19:00:18] Task testsystem.tasks.generate_test_from_screenshot[abc123] succeeded in 3.2s
```

### Через Flower (опционально)

Flower — веб-интерфейс для мониторинга Celery:

```bash
# Установите Flower
pip install flower

# Запустите
celery -A autotest_ui flower
```

Откройте в браузере:

```
http://localhost:5555
```

### Типы задач в системе

1. **generate_test_from_screenshot** — анализ эталонного скриншота
2. **compare_reference_with_actual** — сравнение эталона и актуала
3. **send_ci_callback** — отправка callback в CI/CD систему
4. **create_jira_issue** — создание задачи в Jira

---

## 🔄 Управление версиями эталонов

### Просмотр версий

1. Перейдите в **TESTSYSTEM → Test Case Versions**
2. Вы увидите список всех версий эталонов

### Фильтрация версий

- По причине обновления (manual, design_change, bug_fix)
- По дате создания

### Просмотр истории конкретного тест-кейса

1. Откройте тест-кейс для редактирования
2. В админке версии отображаются в связанных объектах

Или используйте API:

```bash
GET /api/testcases/{id}/version-history/
```

### Управление запросами на обновление

1. Перейдите в **TESTSYSTEM → Reference Update Requests**
2. Вы увидите все запросы с цветными бейджами:
   - 🟡 **Pending** — ожидает проверки
   - 🟢 **Approved** — одобрен
   - 🔴 **Rejected** — отклонён

### Массовое одобрение/отклонение

1. Выберите нужные запросы (чекбоксы)
2. В выпадающем списке **"Action"** выберите:
   - **"Одобрить выбранные запросы"**
   - **"Отклонить выбранные запросы"**
3. Нажмите **"Go"**

---

## 🛠️ Дополнительные возможности

### Экспорт данных

В списке объектов вы можете:
1. Выбрать объекты
2. Action → Export selected
3. Получить CSV/JSON файл

### Массовые операции

Многие админ-панели поддерживают массовые действия:
- Удаление
- Изменение статуса
- Экспорт

### Inline-редактирование

Некоторые связанные объекты можно редактировать прямо на странице родителя:
- UI Elements внутри TestCase
- Defects внутри Run

---

## 🔒 Безопасность

### Лучшие практики

1. **Используйте надёжные пароли**:
   - Минимум 12 символов
   - Буквы (верхний и нижний регистр) + цифры + спецсимволы

2. **Ограничьте права пользователей**:
   - Не давайте Superuser всем подряд
   - Используйте Group permissions для групповых прав

3. **Регулярно проверяйте логи**:
   - Отслеживайте подозрительную активность
   - Используйте `django-admin-logs` для аудита

4. **Включите HTTPS** в продакшене:
   ```python
   # settings.py
   SECURE_SSL_REDIRECT = True
   SESSION_COOKIE_SECURE = True
   CSRF_COOKIE_SECURE = True
   ```

---

## 🚨 Частые проблемы

### Проблема: Не могу войти в админку

**Решение:**

```bash
# Проверьте, что пользователь — staff
python manage.py shell
>>> from django.contrib.auth import get_user_model
>>> User = get_user_model()
>>> user = User.objects.get(username='admin')
>>> user.is_staff = True
>>> user.is_superuser = True
>>> user.save()
>>> exit()
```

### Проблема: Аналитика не загружается

**Решение:**

1. Проверьте, что в БД есть данные
2. Проверьте логи сервера на ошибки
3. Попробуйте другой период (меньше дней)

### Проблема: Celery задачи не выполняются

**Решение:**

```bash
# Проверьте, что Celery worker запущен
celery -A autotest_ui worker --loglevel=info

# Проверьте, что Redis/RabbitMQ запущены
# Для Redis:
redis-cli ping
# Должен вернуть: PONG
```

---

## 📞 Поддержка

Если возникли вопросы:

1. Проверьте документацию проекта
2. Посмотрите логи:
   - Django: console output
   - Celery: `celery worker` output
3. Создайте issue на GitHub

---

**Версия документа:** 1.0  
**Дата обновления:** 2025-12-05
