# Что такое Celery и как он работает в проекте

## 🤔 Что такое Celery?

**Celery** — это система распределенных задач для Python, которая позволяет выполнять длительные операции в фоновом режиме, не блокируя основной веб-сервер.

### Зачем нужен Celery?

Представьте ситуацию:
- Пользователь загружает скриншот и нажимает "Анализ"
- Система должна:
  1. Загрузить изображение
  2. Запустить YOLOv8 (может занять 1-5 секунд)
  3. Обработать результаты
  4. Сохранить в базу данных

**Без Celery:** Пользователь ждет 5 секунд, пока все выполнится. Если операция долгая, браузер может показать таймаут.

**С Celery:** Задача ставится в очередь, пользователь сразу получает ответ "Задача поставлена в очередь", а обработка происходит в фоне.

---

## 🔧 Как Celery работает в этом проекте

### Архитектура

```
┌─────────────┐         ┌──────────┐         ┌─────────────┐
│   Django    │ ──────> │  Redis   │ <────── │   Celery   │
│   Server    │         │ (Broker) │         │   Worker   │
└─────────────┘         └──────────┘         └─────────────┘
     │                        │                    │
     │ 1. Создает задачу      │                    │
     ├────────────────────────>│                    │
     │                        │                    │
     │                        │ 2. Берет задачу    │
     │                        ├───────────────────>│
     │                        │                    │
     │                        │                    │ 3. Выполняет
     │                        │                    │    (YOLOv8, CV)
     │                        │                    │
     │                        │ 4. Сохраняет       │
     │                        │<───────────────────┤
     │                        │    результат       │
     │                        │                    │
```

### Компоненты

1. **Django Server** — веб-сервер, принимает запросы
2. **Redis** — брокер сообщений (хранит очередь задач)
3. **Celery Worker** — воркер, который выполняет задачи

---

## 📋 Какие задачи выполняются через Celery

В проекте есть две основные задачи:

### 1. `generate_test_from_screenshot` 
**Что делает:**
- Загружает эталонный скриншот
- Запускает YOLOv8 для детектирования элементов
- Классифицирует элементы (button, input, label)
- Извлекает текст через OCR
- Сохраняет элементы в базу данных

**Время выполнения:** 2-10 секунд (зависит от размера изображения)

### 2. `compare_reference_with_actual`
**Что делает:**
- Загружает эталонный и актуальный скриншоты
- Выравнивает изображения
- Сравнивает с помощью SSIM
- Анализирует различия элементов
- Создает дефекты
- Вычисляет метрики покрытия

**Время выполнения:** 3-15 секунд

---

## ⚙️ Режимы работы

### Режим 1: Синхронный (по умолчанию)

**Настройка:** `TASKS_FORCE_SYNC=1` в `.env`

**Как работает:**
- Задачи выполняются сразу в том же процессе Django
- Пользователь ждет результата
- Не нужен Redis и Celery Worker

**Плюсы:**
- ✅ Проще настроить
- ✅ Не нужен Redis
- ✅ Результат сразу доступен

**Минусы:**
- ❌ Блокирует веб-сервер
- ❌ При долгих операциях может быть таймаут

**Когда использовать:**
- Для разработки
- Для небольших проектов
- На Windows (где Celery сложнее настроить)

### Режим 2: Асинхронный (через Celery)

**Настройка:** `TASKS_FORCE_SYNC=0` в `.env`

**Как работает:**
- Задачи ставятся в очередь Redis
- Celery Worker берет задачи из очереди и выполняет
- Пользователь получает task_id и может проверить статус

**Плюсы:**
- ✅ Не блокирует веб-сервер
- ✅ Можно масштабировать (несколько воркеров)
- ✅ Задачи выполняются параллельно

**Минусы:**
- ❌ Нужен Redis
- ❌ Нужно запускать Celery Worker отдельно
- ❌ Сложнее настроить

**Когда использовать:**
- Для продакшена
- Для больших проектов
- Когда нужна высокая производительность

---

## 🚀 Как запустить Celery

### Шаг 1: Установите Redis

**Windows (Docker):**
```bash
docker run -d -p 6379:6379 redis:7-alpine
```

**Linux:**
```bash
sudo apt-get install redis-server
redis-server
```

### Шаг 2: Настройте `.env`

```env
TASKS_FORCE_SYNC=0
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
```

### Шаг 3: Запустите Celery Worker

**Windows:**
```powershell
.\start_celery.bat
# или
cd autotest_ui
celery -A autotest_ui worker -l info --pool=solo
```

**Linux/Mac:**
```bash
cd autotest_ui
celery -A autotest_ui worker -l info
```

### Шаг 4: Запустите Django сервер

```bash
cd autotest_ui
python manage.py runserver
```

---

## 🔄 Как это работает в коде

### Пример: Анализ тест-кейса

```python
# views_web.py
def analyze_testcase(request, testcase_id):
    # Запускаем задачу через task_runner
    task_result = run_task_with_fallback(
        generate_test_from_screenshot, 
        testcase_id
    )
    
    if task_result.is_sync:
        # Задача выполнена сразу
        messages.success(request, 'Анализ выполнен!')
    else:
        # Задача поставлена в очередь
        messages.success(request, f'Задача в очереди: {task_result.task_id}')
```

### task_runner.py

```python
def run_task_with_fallback(task, *args, **kwargs):
    # Проверяем настройку TASKS_FORCE_SYNC
    if TASKS_FORCE_SYNC:
        # Выполняем синхронно
        return _run_sync(task, *args, **kwargs)
    else:
        # Ставим в очередь Celery
        async_result = task.apply_async(args=args, kwargs=kwargs)
        return TaskExecutionResult(mode='async', task_id=async_result.id)
```

---

## 📊 Мониторинг задач

### Проверка статуса задачи

```python
from celery.result import AsyncResult

task_id = "abc-123-def"
result = AsyncResult(task_id)

print(result.state)  # PENDING, STARTED, SUCCESS, FAILURE
print(result.result)  # Результат выполнения
```

### Просмотр очереди (через Redis CLI)

```bash
redis-cli
> LLEN celery  # Количество задач в очереди
> LRANGE celery 0 -1  # Список задач
```

---

## ⚠️ Важные замечания

1. **По умолчанию используется синхронный режим** (`TASKS_FORCE_SYNC=1`)
   - Это упрощает разработку
   - Не нужен Redis и Celery Worker

2. **Fallback режим:**
   - Если Celery недоступен, задачи выполняются синхронно
   - Настройка: `TASKS_FALLBACK_TO_SYNC=1`

3. **Windows:**
   - Celery на Windows требует `--pool=solo`
   - Рекомендуется использовать синхронный режим для разработки

---

## 🎯 Рекомендации

### Для разработки:
```env
TASKS_FORCE_SYNC=1  # Синхронный режим
```

### Для продакшена:
```env
TASKS_FORCE_SYNC=0  # Асинхронный режим
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
```

---

**Итого:** Celery позволяет выполнять долгие операции (YOLOv8, сравнение изображений) в фоне, не блокируя веб-сервер. По умолчанию проект работает в синхронном режиме для простоты, но можно переключиться на асинхронный для лучшей производительности.

