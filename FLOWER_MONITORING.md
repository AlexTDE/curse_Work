# 🌸 Flower - Celery Monitoring

Flower - это веб-интерфейс для мониторинга и управления Celery задачами в реальном времени.

---

## 🚀 Быстрый старт

### 1. Установка

```bash
# Установить зависимости (Flower уже в requirements.txt)
pip install -r requirements.txt
```

### 2. Запуск

**Linux/Mac:**
```bash
chmod +x start_flower.sh
./start_flower.sh
```

**Windows:**
```bash
start_flower.bat
```

**Или вручную:**
```bash
cd autotest_ui
celery -A autotest_ui flower --port=5555 --basic_auth=admin:admin123
```

### 3. Доступ

🌐 **URL:** http://localhost:5555

🔐 **Логин:** `admin`  
🔑 **Пароль:** `admin123`

---

## 📊 Возможности Flower

### 1. **Dashboard** - Главная панель
- Общее количество задач
- Активные задачи в реальном времени
- Статистика успеха/неудач
- Графики производительности

### 2. **Tasks** - Мониторинг задач
- ✅ Список всех выполненных задач
- ⏳ Активные задачи (в процессе выполнения)
- ❌ Неудачные задачи с трассировкой ошибок
- ⏱️ Время выполнения каждой задачи
- 🔍 Детальная информация о параметрах и результатах

### 3. **Workers** - Управление воркерами
- 📈 Список активных Celery воркеров
- 💻 Использование CPU и памяти
- 🔄 Количество выполненных задач на воркер
- ⚙️ Конфигурация воркеров
- 🛑 Возможность остановки/перезапуска воркеров

### 4. **Monitor** - Графики в реальном времени
- 📊 Количество задач по времени
- ⚡ Скорость выполнения задач
- 🎯 Успешность выполнения
- 🔥 Нагрузка на систему

### 5. **Broker** - Информация о брокере (Redis)
- 📡 Статус подключения
- 📦 Очереди задач
- 🔢 Количество задач в очереди

---

## 🎯 Основные задачи системы

В нашей системе автотестирования UI есть следующие Celery задачи:

### 1. `generate_test_from_screenshot`
**Назначение:** Анализ эталонного скриншота и извлечение UI-элементов

**Параметры:**
- `testcase_id` - ID тест-кейса

**Что происходит:**
- Загрузка изображения
- Обнаружение UI-элементов (кнопки, поля ввода, текст)
- Классификация элементов
- OCR распознавание текста
- Сохранение в БД

**Время выполнения:** 5-30 секунд (зависит от размера изображения)

### 2. `compare_reference_with_actual`
**Назначение:** Сравнение эталонного и актуального скриншотов

**Параметры:**
- `run_id` - ID прогона

**Что происходит:**
- Загрузка обоих изображений
- Вычисление SSIM (структурное сходство)
- Поиск различий в UI-элементах
- Создание метрики покрытия
- Регистрация дефектов

**Время выполнения:** 10-60 секунд

---

## 🔍 Как использовать для отладки

### Проверка выполнения задачи

1. **Перейдите в раздел "Tasks"**
2. **Найдите свою задачу** по имени или времени
3. **Кликните на задачу** для просмотра деталей

**Вы увидите:**
- ✅ Статус (SUCCESS, FAILURE, PENDING, STARTED)
- ⏱️ Время начала и окончания
- 📝 Аргументы задачи
- 📤 Результат выполнения
- ❌ Traceback ошибки (если была)

### Пример: Отладка задачи анализа

```python
# В вашем коде вы создаёте задачу:
task_result = generate_test_from_screenshot.delay(testcase_id=42)

# Получаем task_id
print(f"Task ID: {task_result.id}")
```

Теперь в Flower:
1. Перейдите в **Tasks**
2. Найдите задачу по **Task ID**
3. Посмотрите статус и детали

---

## 📈 Мониторинг производительности

### Метрики для отслеживания:

1. **Task Success Rate** - % успешных задач
   - ✅ Хорошо: > 95%
   - ⚠️ Нужно внимание: 80-95%
   - ❌ Проблема: < 80%

2. **Average Task Runtime** - среднее время выполнения
   - ✅ `generate_test_from_screenshot`: < 30 сек
   - ✅ `compare_reference_with_actual`: < 60 сек

3. **Worker Load** - нагрузка на воркеры
   - ✅ < 80% CPU
   - ✅ < 80% RAM

4. **Queue Length** - задачи в очереди
   - ✅ < 10 задач
   - ⚠️ 10-50 задач (нужно добавить воркеры)
   - ❌ > 50 задач (критическая нагрузка)

---

## 🛠️ Управление задачами

### Отмена задачи

1. Найдите задачу в списке **Tasks**
2. Нажмите **Revoke** (отменить)
3. Задача будет остановлена

### Перезапуск неудачной задачи

1. Найдите задачу со статусом **FAILURE**
2. Посмотрите ошибку в **Traceback**
3. Исправьте код
4. Запустите задачу заново из вашего приложения

---

## ⚙️ Настройки Flower

### Изменить порт

```bash
celery -A autotest_ui flower --port=8888
```

### Изменить логин/пароль

```bash
celery -A autotest_ui flower --basic_auth=myuser:mypass
```

### Отключить аутентификацию (не рекомендуется для продакшена)

```bash
celery -A autotest_ui flower --port=5555
```

### Хранить больше истории задач

```bash
celery -A autotest_ui flower --max_tasks=50000
```

---

## 🔒 Безопасность

### В продакшене:

1. **Обязательно используйте аутентификацию:**
   ```bash
   --basic_auth=admin:STRONG_PASSWORD
   ```

2. **Используйте HTTPS** (настройте reverse proxy через Nginx)

3. **Ограничьте доступ по IP** (через firewall)

4. **Не открывайте порт наружу** без защиты

---

## 📊 Интеграция с админкой Django

Вы можете добавить ссылку на Flower в админку:

1. Откройте `testsystem/admin.py`
2. В `CustomAdminSite` добавьте ссылку:

```python
class CustomAdminSite(admin.AdminSite):
    # ... существующий код ...
    
    def each_context(self, request):
        context = super().each_context(request)
        context['flower_url'] = 'http://localhost:5555'
        return context
```

3. В шаблоне админки добавьте ссылку:

```html
<a href="{{ flower_url }}" target="_blank">
    🌸 Мониторинг Celery (Flower)
</a>
```

---

## 🐛 Решение проблем

### Flower не запускается

**Проблема:** `ImportError: cannot import name 'soft_unicode'`

**Решение:** Обновите Flower:
```bash
pip install --upgrade flower
```

### Не видны задачи

**Проблема:** В Flower пусто, но задачи выполняются

**Решение:** 
1. Проверьте, что Celery worker запущен
2. Проверьте, что Redis работает
3. Убедитесь, что `--broker` указан правильно

### Забыли пароль

**Решение:** Перезапустите Flower без аутентификации:
```bash
celery -A autotest_ui flower --port=5555
```

---

## 📚 Полезные ссылки

- [Официальная документация Flower](https://flower.readthedocs.io/)
- [Celery Documentation](https://docs.celeryproject.org/)
- [GitHub репозиторий Flower](https://github.com/mher/flower)

---

## ✅ Чек-лист для администратора

- [ ] Установлен Flower (`pip install flower`)
- [ ] Flower запущен и доступен по http://localhost:5555
- [ ] Настроена аутентификация (`--basic_auth`)
- [ ] Celery worker запущен и виден в разделе **Workers**
- [ ] Задачи отображаются в разделе **Tasks**
- [ ] Проверена работа задач (создать тест-кейс и запустить анализ)
- [ ] Настроен мониторинг критических метрик
- [ ] Документирован процесс запуска для команды

---

**🎉 Теперь у вас есть полноценный мониторинг Celery задач!**
