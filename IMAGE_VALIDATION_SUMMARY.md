# Сводка по валидации изображений

## Что было реализовано

Добавлена **многоуровневая валидация** загружаемых изображений на всех уровнях приложения:

### 1. Клиентская валидация (JavaScript)

**Файл:** `autotest_ui/testsystem/templates/testsystem/testcases_list.html`

Проверки перед отправкой формы:

- ✅ **Проверка размера**: максимум 10 MB
- ✅ **Проверка MIME-типа**: `image/jpeg`, `image/png`, `image/gif`, etc.
- ✅ **Проверка расширения**: `.jpg`, `.jpeg`, `.png`, `.gif`, `.bmp`, `.webp`, `.tiff`
- ✅ **Превью изображения**: показ миниатюры перед загрузкой
- ✅ **Блокировка кнопки**: кнопка "Создать" отключается при невалидном файле
- ✅ **Визуальные ошибки**: красный текст под полем ввода

**Пример ошибки:**
```
Ошибка: Неподдерживаемый формат файла "application/pdf". 
Разрешены только изображения: JPG, JPEG, PNG, GIF, BMP, WebP, TIFF
```

### 2. Серверная валидация (уровень моделей)

**Файл:** `autotest_ui/testsystem/validators.py`

Функция `validate_image_file()` выполняет:

1. **Проверка расширения**
   ```python
   valid_extensions = ['jpg', 'jpeg', 'png', 'gif', 'bmp', 'webp', 'tiff', 'tif']
   ```

2. **Проверка MIME-типа**
   ```python
   valid_mime_types = [
       'image/jpeg', 'image/jpg', 'image/png', 'image/gif',
       'image/bmp', 'image/webp', 'image/tiff'
   ]
   ```

3. **Проверка фактического содержимого**
   - Использует `imghdr.what()` для определения реального формата
   - Защита от переименования `file.pdf` в `file.jpg`

4. **Проверка размера**
   ```python
   max_size = 10 * 1024 * 1024  # 10 MB
   ```

### 3. Валидация на уровне моделей

**Файл:** `autotest_ui/testsystem/models.py`

Валидатор применяется к полям:

```python
class TestCase(models.Model):
    reference_screenshot = models.ImageField(
        upload_to='references/',
        validators=[validate_image_file],
        help_text='Только изображения: JPG, JPEG, PNG, GIF, BMP, WebP, TIFF (макс. 10 MB)'
    )

class Run(models.Model):
    actual_screenshot = models.ImageField(
        upload_to='runs/',
        validators=[validate_image_file],
        help_text='Только изображения: JPG, JPEG, PNG, GIF, BMP, WebP, TIFF (макс. 10 MB)'
    )

class Defect(models.Model):
    screenshot = models.ImageField(
        upload_to='defects/',
        validators=[validate_image_file],
        help_text='Только изображения: JPG, JPEG, PNG, GIF, BMP, WebP, TIFF (макс. 10 MB)'
    )
```

### 4. Валидация в DRF сериализаторах

**Файл:** `autotest_ui/testsystem/serializers.py`

```python
class TestCaseSerializer(serializers.ModelSerializer):
    def validate_reference_screenshot(self, value):
        if value:
            try:
                validate_image_file(value)
            except DjangoValidationError as e:
                raise serializers.ValidationError(e.messages)
        return value

class RunSerializer(serializers.ModelSerializer):
    def validate_actual_screenshot(self, value):
        if not value:
            raise serializers.ValidationError("Скриншот обязателен")
        try:
            validate_image_file(value)
        except DjangoValidationError as e:
            raise serializers.ValidationError(e.messages)
        return value
```

### 5. Валидация в Django формах

**Файл:** `autotest_ui/testsystem/forms.py`

```python
class TestCaseForm(forms.ModelForm):
    def clean_reference_screenshot(self):
        screenshot = self.cleaned_data.get('reference_screenshot')
        if screenshot:
            try:
                validate_image_file(screenshot)
            except ValidationError as e:
                raise forms.ValidationError(e.messages)
        elif not self.instance.pk:
            raise forms.ValidationError('Эталонный скриншот обязателен')
        return screenshot
```

### 6. Валидация в API представлениях

**Файл:** `autotest_ui/testsystem/views.py`

```python
class TestCaseViewSet(viewsets.ModelViewSet):
    def perform_create(self, serializer):
        reference_screenshot = serializer.validated_data.get('reference_screenshot')
        if reference_screenshot:
            try:
                validate_image_file(reference_screenshot)
            except DjangoValidationError as e:
                raise ValidationError({'reference_screenshot': e.messages})
        serializer.save(created_by=self.request.user)
```

## Как это работает

### Сценарий 1: Попытка загрузить PDF через веб-интерфейс

1. Пользователь выбирает `document.pdf`
2. **JavaScript** немедленно проверяет MIME-тип
3. Показывается красное сообщение об ошибке
4. Кнопка "Создать" блокируется
5. Поле ввода очищается
6. **Файл не отправляется на сервер**

### Сценарий 2: Попытка переименовать PDF в JPG

1. Пользователь переименовал `document.pdf` в `fake.jpg`
2. JavaScript пропускает (расширение валидное)
3. Форма отправляется на сервер
4. **Серверный валидатор** проверяет фактическое содержимое через `imghdr.what()`
5. Обнаруживает, что это PDF, а не JPEG
6. Возвращает HTTP 400 с ошибкой
7. **Тест-кейс НЕ создаётся**
8. Пользователь видит ошибку: "Файл не является корректным изображением"

### Сценарий 3: Загрузка корректного изображения

1. Пользователь выбирает `screenshot.png`
2. JavaScript проверяет: MIME-тип ✅, расширение ✅, размер ✅
3. Показывается превью изображения
4. Кнопка "Создать" активна
5. Форма отправляется на сервер
6. Сервер проверяет и подтверждает валидность
7. **Тест-кейс успешно создаётся** ✅
8. Файл сохраняется в `media/references/`

## Преимущества решения

### 🔒 Безопасность

- Невозможно загрузить вредоносные файлы под видом изображений
- Защита от переполнения диска (лимит 10 MB)
- Многоуровневая проверка (клиент + сервер)

### 📊 UX (Пользовательский опыт)

- Мгновенная обратная связь (нет ожидания ответа сервера)
- Превью изображения перед загрузкой
- Понятные сообщения об ошибках на русском языке
- Визуальное выделение ошибок

### 🛠️ Надёжность

- Двойная проверка (клиент + сервер)
- Работает даже если JavaScript отключён
- Не полагается только на расширение файла
- Проверяет фактическое содержимое

## Разрешённые форматы

✅ JPG / JPEG  
✅ PNG  
✅ GIF  
✅ BMP  
✅ WebP  
✅ TIFF  

❌ PDF  
❌ TXT  
❌ DOC / DOCX  
❌ ZIP / RAR  
❌ EXE / DLL  
❌ Любые не-изображения  

## Тестирование

### Тест 1: Клиентская валидация

1. Откройте страницу создания тест-кейса
2. Попробуйте загрузить `.pdf` файл
3. Проверьте, что появилось красное сообщение об ошибке
4. Проверьте, что кнопка "Создать" заблокирована

### Тест 2: Серверная валидация (API)

```bash
curl -X POST http://localhost:8000/api/testcases/ \
  -H "Authorization: Token YOUR_TOKEN" \
  -F "title=Test" \
  -F "reference_screenshot=@document.pdf"
```

**Ожидаемый результат:**
```json
{
  "reference_screenshot": [
    "Недопустимый тип файла 'application/pdf'. Файл не является изображением."
  ]
}
```

HTTP статус: `400 Bad Request`

### Тест 3: Защита от переименования

```bash
# Переименуйте PDF в JPG
cp document.pdf fake.jpg

curl -X POST http://localhost:8000/api/testcases/ \
  -H "Authorization: Token YOUR_TOKEN" \
  -F "title=Test" \
  -F "reference_screenshot=@fake.jpg"
```

**Ожидаемый результат:** HTTP 400 + ошибка валидации

## Что делать дальше

1. **Обновите код:**
   ```bash
   git pull origin main
   ```

2. **Примените миграции:**
   ```bash
   python manage.py makemigrations
   python manage.py migrate
   ```

3. **Перезапустите сервер:**
   ```bash
   python manage.py runserver
   ```

4. **Протестируйте:**
   - Попробуйте загрузить PDF
   - Проверьте, что появляется ошибка
   - Загрузите корректное изображение
   - Убедитесь, что тест-кейс создаётся

## Заключение

Теперь ваша система **полностью защищена** от загрузки не-изображений:

✅ Клиентская валидация (JavaScript)  
✅ Серверная валидация (модели)  
✅ API валидация (DRF)  
✅ Формы Django  
✅ Защита от переименования  
✅ Проверка размера (10 MB)  

**Ни один TXT/PDF файл не проскочит!** 🔒
