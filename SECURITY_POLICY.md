# 🔐 Политика безопасности системы автоматического тестирования UI

**Версия:** 1.0  
**Дата последнего обновления:** 05.12.2025  
**Статус:** Активная

---

## 📋 Содержание

1. [Введение](#введение)
2. [Архитектура безопасности](#архитектура-безопасности)
3. [Защита учетных записей](#защита-учетных-записей)
4. [Контроль доступа](#контроль-доступа)
5. [Защита данных](#защита-данных)
6. [Сетевая безопасность](#сетевая-безопасность)
7. [Защита от веб-уязвимостей](#защита-от-веб-уязвимостей)
8. [Конфигурационная безопасность](#конфигурационная-безопасность)
9. [Мониторинг и логирование](#мониторинг-и-логирование)
10. [Резервное копирование](#резервное-копирование)
11. [Интеграции и API](#интеграции-и-api)
12. [Процедуры реагирования](#процедуры-реагирования)
13. [Соответствие стандартам](#соответствие-стандартам)

---

## Введение

### Цель документа

Данный документ определяет политику информационной безопасности системы автоматического тестирования UI, описывает реализованные меры защиты и регламентирует процедуры обеспечения безопасности.

### Область применения

Политика применяется ко всем компонентам системы:
- Django веб-приложение
- PostgreSQL база данных
- Redis брокер сообщений
- Celery фоновые задачи
- REST API
- CI/CD интеграции
- Файловое хранилище

### Принципы безопасности

1. **Defense in Depth** (эшелонированная защита) - многоуровневая защита
2. **Least Privilege** (минимальные привилегии) - доступ только к необходимым ресурсам
3. **Secure by Default** (безопасность по умолчанию) - безопасные настройки из коробки
4. **Privacy by Design** (конфиденциальность на этапе проектирования) - минимизация данных
5. **Fail Secure** (безопасный отказ) - при ошибках система блокирует доступ

---

## Архитектура безопасности

### Компоненты системы

```
┌─────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                       │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  Layer 7: Network Security (HTTPS, CORS, Firewall)      │
│  ────────────────────────────────────────────────────   │
│  Layer 6: Application Security (CSRF, XSS, SQLi)        │
│  ────────────────────────────────────────────────────   │
│  Layer 5: Authentication (Argon2, Session, Token)       │
│  ────────────────────────────────────────────────────   │
│  Layer 4: Authorization (Permissions, Roles)            │
│  ────────────────────────────────────────────────────   │
│  Layer 3: Data Protection (Encryption, Hashing)         │
│  ────────────────────────────────────────────────────   │
│  Layer 2: Input Validation (Forms, Serializers)         │
│  ────────────────────────────────────────────────────   │
│  Layer 1: Database Security (Constraints, Isolation)    │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

---

## Защита учетных записей

### 1. Хеширование паролей

#### Используемые алгоритмы (приоритет)

```python
PASSWORD_HASHERS = [
    'django.contrib.auth.hashers.Argon2PasswordHasher',      # Приоритет 1
    'django.contrib.auth.hashers.PBKDF2PasswordHasher',      # Приоритет 2
    'django.contrib.auth.hashers.PBKDF2SHA1PasswordHasher',  # Приоритет 3
]
```

#### Argon2 (основной алгоритм)

**Характеристики:**
- ✅ Победитель Password Hashing Competition 2015
- ✅ Рекомендация OWASP 2024
- ✅ Memory-hard функция (защита от GPU-атак)
- ✅ Настраиваемые параметры: memory cost, time cost, parallelism

**Параметры по умолчанию:**
- Memory cost: 102,400 KB (100 MB)
- Time cost: 2 iterations
- Parallelism: 8 threads
- Variant: Argon2id (гибрид Argon2i и Argon2d)

**Формат хранения:**
```
argon2$argon2id$v=19$m=102400,t=2,p=8$<salt>$<hash>
```

**Защита от атак:**
- 🛡️ Brute Force: ~0.5 секунды на 1 попытку
- 🛡️ Rainbow Tables: уникальная соль для каждого пароля
- 🛡️ GPU/ASIC: требует много RAM, непрактично на GPU
- 🛡️ Timing Attacks: constant-time comparison

#### PBKDF2-SHA256 (резервный алгоритм)

**Характеристики:**
- ✅ Стандарт NIST SP 800-132
- ✅ 260,000 итераций (OWASP рекомендует 100,000+)
- ✅ SHA-256 hash функция
- ✅ Случайная соль 128 бит

**Формат хранения:**
```
pbkdf2_sha256$260000$<salt>$<hash>
```

#### Автоматическое улучшение хешей

```python
# При входе пользователя Django автоматически проверяет:
if user.password.startswith('pbkdf2_'):
    # Обновить до Argon2
    user.set_password(raw_password)
    user.save()
```

### 2. Валидация паролей

#### Применяемые валидаторы

**1. UserAttributeSimilarityValidator**
- Проверяет схожесть пароля с username, email, именем
- Предотвращает использование личной информации в пароле

```python
❌ username: john_doe, password: johndoe123
✅ username: john_doe, password: Kx9$mP2qL!vR
```

**2. MinimumLengthValidator**
- Минимальная длина: **8 символов**
- Соответствует NIST SP 800-63B

```python
OPTIONS = {'min_length': 8}
```

**3. CommonPasswordValidator**
- База: 20,000+ распространенных паролей
- Обновляется из Have I Been Pwned

```python
❌ password, 123456, qwerty, admin
✅ MyS3cur3P@ssw0rd!
```

**4. NumericPasswordValidator**
- Запрещает полностью числовые пароли
- Требует как минимум 1 не-цифровой символ

```python
❌ 12345678
✅ Pass1234
```

### 3. Управление сессиями

#### Настройки сессий

```python
# settings.py (рекомендуется для production)
SESSION_COOKIE_SECURE = True          # Только HTTPS
SESSION_COOKIE_HTTPONLY = True        # Недоступна для JavaScript
SESSION_COOKIE_SAMESITE = 'Lax'       # CSRF защита
SESSION_COOKIE_AGE = 1209600           # 2 недели
SESSION_SAVE_EVERY_REQUEST = False     # Экономия ресурсов
SESSION_EXPIRE_AT_BROWSER_CLOSE = False
```

#### Безопасность сессий

- ✅ Session ID хранится только в cookie
- ✅ Автоматическая ротация session ID при входе
- ✅ Инвалидация сессии при выходе
- ✅ Backend: database (защита от race conditions)

---

## Контроль доступа

### 1. Аутентификация

#### Методы аутентификации

**Веб-интерфейс:**
```python
SessionAuthentication  # Cookie-based
```

**REST API:**
```python
SessionAuthentication  # Cookie для браузера
BasicAuthentication    # Username:Password для API клиентов
```

#### Обязательная аутентификация

```python
# Все views защищены декоратором
@login_required
def my_view(request):
    pass

# API endpoints
permission_classes = [IsAuthenticated]
```

### 2. Авторизация

#### Модель разрешений

**Роли:**
- **Суперпользователь** (`is_superuser=True`): полный доступ
- **Администратор** (`is_staff=True`): доступ к Django Admin
- **Пользователь** (`is_active=True`): доступ к своим данным

#### Разделение доступа к данным

**Принцип:** Пользователи видят только свои объекты.

```python
# Модели с владельцем
class TestCase(models.Model):
    created_by = models.ForeignKey(User, on_delete=models.CASCADE)

# Фильтрация в views
testcases = TestCase.objects.filter(created_by=request.user)

# Проверка владельца при удалении
if testcase.created_by != request.user:
    return HttpResponseForbidden()
```

**Исключение:** Администраторы видят все данные для управления.

#### Матрица доступа

| Объект | Просмотр | Создание | Редактирование | Удаление |
|--------|----------|----------|----------------|----------|
| **Собственные тест-кейсы** | ✅ Владелец | ✅ Владелец | ✅ Владелец | ✅ Владелец |
| **Чужие тест-кейсы** | ❌ | ❌ | ❌ | ❌ |
| **Все тест-кейсы** | ✅ Админ | ✅ Админ | ✅ Админ | ✅ Админ |
| **Пользователи** | ❌ | ✅ Регистрация | ✅ Свой профиль | ✅ Админ |
| **Настройки системы** | ✅ Админ | ✅ Админ | ✅ Админ | ✅ Админ |

### 3. URL-based Access Control

```python
# urls.py - разделение по правам

# Публичные (без аутентификации)
path('login/', ...)
path('api/webhooks/', ...)  # С проверкой подписи

# Требуют аутентификации
@login_required
path('testcases/', ...)
path('api/testcases/', ...)

# Только администраторы
@staff_member_required
path('admin/', admin.site.urls)
path('analytics/', ...)
```

---

## Защита данных

### 1. Минимизация сбора данных

#### Собираемые данные

**Обязательные:**
- Username (произвольный логин)
- Password (только хеш)

**Опциональные:**
- Email (может быть пустым)
- Имя, Фамилия (не обязательны)

**Системные:**
- Дата регистрации
- Дата последнего входа
- Активность (is_active)

#### Принцип минимизации

```python
✅ Собираем: Только необходимое для работы системы
❌ Не собираем: Телефон, адрес, дата рождения, IP-адреса
```

### 2. Хранение данных

#### База данных

**PostgreSQL Security:**

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'CONN_MAX_AGE': 600,           # Connection pooling
        'OPTIONS': {
            'connect_timeout': 10,      # Защита от зависания
        },
    }
}
```

**Меры безопасности:**
- ✅ Подключение только с localhost (production: VPN/firewall)
- ✅ Сильный пароль для PostgreSQL
- ✅ Отдельный пользователь БД для приложения
- ✅ Connection pooling для защиты от исчерпания соединений
- ✅ Транзакции для целостности данных

#### Файловое хранилище

**Скриншоты и изображения:**

```python
MEDIA_ROOT = BASE_DIR / 'media'
MEDIA_URL = '/media/'
```

**Защита файлов:**
- ✅ Автоматическое удаление при удалении объекта (Django signals)
- ✅ Хранение вне web root (доступ через Django views)
- ✅ Валидация типов файлов (только изображения)
- ✅ Ограничение размера загружаемых файлов

```python
# Автоматическое удаление файлов
@receiver(models.signals.post_delete, sender=UIElement)
def auto_delete_file_on_delete(sender, instance, **kwargs):
    if instance.screenshot:
        if os.path.isfile(instance.screenshot.path):
            os.remove(instance.screenshot.path)
```

### 3. Удаление данных

#### Каскадное удаление

**При удалении пользователя:**
```python
User (deleted)
  └── TestCase.objects.filter(created_by=user) → CASCADE DELETE
        ├── UIElement.objects.filter(testcase=tc) → CASCADE DELETE
        │     └── screenshot files → AUTO DELETE (signal)
        ├── TestRun.objects.filter(testcase=tc) → CASCADE DELETE
        │     ├── expected_screenshot → AUTO DELETE
        │     ├── actual_screenshot → AUTO DELETE
        │     └── diff_image → AUTO DELETE
        └── Defect.objects.filter(testcase=tc) → CASCADE DELETE
```

**Гарантии:**
- ✅ Все связанные данные удаляются
- ✅ Файлы удаляются с диска
- ✅ Нет orphaned records
- ✅ Транзакционность (rollback при ошибке)

---

## Сетевая безопасность

### 1. HTTPS/TLS

#### Production настройки

```python
# settings.py (для production)
SECURE_SSL_REDIRECT = True              # HTTP → HTTPS редирект
SECURE_HSTS_SECONDS = 31536000          # 1 год HSTS
SECURE_HSTS_INCLUDE_SUBDOMAINS = True
SECURE_HSTS_PRELOAD = True
SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')
```

#### Рекомендации для развертывания

- ✅ TLS 1.3 (минимум TLS 1.2)
- ✅ Strong cipher suites только
- ✅ Сертификат от доверенного CA (Let's Encrypt)
- ✅ Автоматическое обновление сертификатов

### 2. CORS (Cross-Origin Resource Sharing)

#### Development режим

```python
DEBUG = True
CORS_ALLOW_ALL_ORIGINS = True  # Только для разработки!
```

#### Production режим

```python
DEBUG = False
CORS_ALLOW_ALL_ORIGINS = False
CORS_ALLOWED_ORIGINS = [
    'https://yourdomain.com',
    'https://app.yourdomain.com',
]
```

**Конфигурация через .env:**
```bash
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
```

### 3. Allowed Hosts

```python
# Development
ALLOWED_HOSTS = ['localhost', '127.0.0.1']

# Production
ALLOWED_HOSTS = ['yourdomain.com', 'www.yourdomain.com']
```

**Конфигурация через .env:**
```bash
DJANGO_ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
```

### 4. CSRF Trusted Origins

```python
CSRF_TRUSTED_ORIGINS = [
    'http://localhost:8000',       # Development
    'https://yourdomain.com',      # Production
]
```

---

## Защита от веб-уязвимостей

### 1. CSRF Protection

**Middleware:**
```python
MIDDLEWARE = [
    'django.middleware.csrf.CsrfViewMiddleware',  # ✅ Включен
]
```

**Механизм защиты:**
- ✅ CSRF token в каждой форме
- ✅ Проверка токена при POST/PUT/DELETE
- ✅ SameSite cookie attribute
- ✅ Referer header validation

**Использование в шаблонах:**
```html
<form method="post">
    {% csrf_token %}
    <!-- form fields -->
</form>
```

**API endpoints:**
```python
# REST API использует SessionAuthentication с CSRF
# Или exempt для webhooks с проверкой подписи
@csrf_exempt
@require_http_methods(["POST"])
def github_webhook(request):
    # Проверка HMAC подписи вместо CSRF
    verify_github_signature(request)
```

### 2. XSS Protection

**Django Template Escaping:**
```python
# Автоматический escaping по умолчанию
{{ user_input }}  # Автоматически экранируется

# Явное отключение (осторожно!)
{{ html_content|safe }}  # Используйте только для проверенного HTML
```

**Content Security Policy (рекомендуется):**
```python
# settings.py
MIDDLEWARE += ['csp.middleware.CSPMiddleware']

CSP_DEFAULT_SRC = ("'self'",)
CSP_SCRIPT_SRC = ("'self'", "'unsafe-inline'")  # Минимизируйте unsafe-inline
CSP_STYLE_SRC = ("'self'", "'unsafe-inline'")
```

### 3. SQL Injection Protection

**Django ORM:**
```python
# ✅ БЕЗОПАСНО - параметризированные запросы
TestCase.objects.filter(name=user_input)

# ❌ ОПАСНО - никогда не делайте так!
# TestCase.objects.raw(f"SELECT * FROM testcase WHERE name='{user_input}'")

# ✅ БЕЗОПАСНО - если нужен raw SQL
TestCase.objects.raw("SELECT * FROM testcase WHERE name=%s", [user_input])
```

**Гарантии Django ORM:**
- ✅ Автоматическая параметризация
- ✅ Экранирование специальных символов
- ✅ Type casting
- ✅ Защита от всех типов SQL injection

### 4. Clickjacking Protection

**Middleware:**
```python
MIDDLEWARE = [
    'django.middleware.clickjacking.XFrameOptionsMiddleware',  # ✅
]
```

**Заголовок:**
```http
X-Frame-Options: DENY
```

**Опции:**
- `DENY` - запрет встраивания в iframe (по умолчанию)
- `SAMEORIGIN` - разрешить только с того же домена

### 5. Security Headers

**Автоматические заголовки Django:**

```python
MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',  # ✅
]
```

**Устанавливаемые заголовки:**
```http
X-Content-Type-Options: nosniff
X-Frame-Options: DENY
X-XSS-Protection: 1; mode=block
Strict-Transport-Security: max-age=31536000; includeSubDomains
```

---

## Конфигурационная безопасность

### 1. Управление секретами

#### Environment Variables (.env файл)

```bash
# .env (НЕ коммитить в Git!)
DJANGO_SECRET_KEY=your-secret-key-here-min-50-chars
DJANGO_DEBUG=0
DJANGO_ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

GITHUB_WEBHOOK_SECRET=your-github-webhook-secret
GITLAB_WEBHOOK_TOKEN=your-gitlab-webhook-token
JENKINS_WEBHOOK_SECRET=your-jenkins-webhook-secret

JIRA_URL=https://your-company.atlassian.net
JIRA_USERNAME=your-email@company.com
JIRA_API_TOKEN=your-jira-api-token
```

**Защита .env:**
```bash
# .gitignore
.env
.env.local
.env.*.local
```

#### SECRET_KEY генерация

```python
# Генерация безопасного ключа
from django.core.management.utils import get_random_secret_key
print(get_random_secret_key())

# Минимум 50 символов!
SECRET_KEY = os.getenv('DJANGO_SECRET_KEY', 'dev-secret-change-me')
```

### 2. DEBUG режим

**Development:**
```python
DEBUG = True  # Показывает подробные ошибки
```

**Production:**
```python
DEBUG = False  # ❗ КРИТИЧНО! Скрывает traceback от пользователей
```

**Опасности DEBUG=True в production:**
- 🚨 Раскрытие структуры проекта
- 🚨 Утечка переменных окружения
- 🚨 Раскрытие SQL запросов
- 🚨 Информация о версиях библиотек

### 3. Конфигурация через переменные окружения

**Все критичные настройки:**

```python
# settings.py
SECRET_KEY = os.getenv('DJANGO_SECRET_KEY', 'dev-secret-change-me')
DEBUG = os.getenv('DJANGO_DEBUG', '1') == '1'
ALLOWED_HOSTS = os.getenv('DJANGO_ALLOWED_HOSTS', 'localhost').split(',')

CELERY_BROKER_URL = os.getenv('CELERY_BROKER_URL', 'redis://localhost:6379/0')

GITHUB_WEBHOOK_SECRET = os.getenv('GITHUB_WEBHOOK_SECRET', '')
JIRA_API_TOKEN = os.getenv('JIRA_API_TOKEN', '')
```

**Преимущества:**
- ✅ Разные конфигурации для dev/staging/production
- ✅ Секреты не в Git
- ✅ Легкое развертывание (12-factor app)

---

## Мониторинг и логирование

### 1. Логирование безопасности

#### Django Logging

```python
# settings.py
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'formatters': {
        'verbose': {
            'format': '{levelname} {asctime} {module} {message}',
            'style': '{',
        },
    },
    'handlers': {
        'file': {
            'level': 'WARNING',
            'class': 'logging.FileHandler',
            'filename': BASE_DIR / 'logs' / 'security.log',
            'formatter': 'verbose',
        },
    },
    'loggers': {
        'django.security': {
            'handlers': ['file'],
            'level': 'WARNING',
            'propagate': False,
        },
    },
}
```

#### Логируемые события

**Автоматически логируются:**
- ❌ Неудачные попытки входа
- ❌ CSRF ошибки
- ❌ 403 Forbidden ошибки
- ❌ 404 Not Found (подозрительные URL)
- ❌ 500 Internal Server Error

**Рекомендуется добавить:**
- ✅ Успешные входы в систему
- ✅ Изменения паролей
- ✅ Создание/удаление пользователей (админами)
- ✅ Критичные операции (удаление данных)

### 2. Мониторинг Celery (Flower)

**Запуск Flower:**
```bash
celery -A autotest_ui flower --port=5555 --basic_auth=admin:SecurePassword123!
```

**Мониторинг:**
- ✅ Активные задачи
- ✅ Статистика выполнения
- ✅ Ошибки с traceback
- ✅ Использование ресурсов

**Безопасность Flower:**
- ✅ Basic Auth для доступа
- ✅ Доступ только с localhost (или VPN)
- ⚠️ НЕ выставлять публично!

### 3. Мониторинг БД

**PostgreSQL логирование:**

```sql
-- postgresql.conf
logging_collector = on
log_directory = 'pg_log'
log_filename = 'postgresql-%Y-%m-%d.log'
log_statement = 'ddl'           # Логировать DDL (CREATE, DROP)
log_connections = on
log_disconnections = on
log_duration = on
log_min_duration_statement = 1000  # Медленные запросы (> 1 сек)
```

---

## Резервное копирование

### 1. База данных

#### Автоматический backup PostgreSQL

```bash
#!/bin/bash
# backup.sh

BACKUP_DIR="/backups/postgresql"
DATE=$(date +"%Y%m%d_%H%M%S")
FILENAME="autotest_ui_$DATE.sql"

pg_dump -U postgres -d autotest_ui > "$BACKUP_DIR/$FILENAME"
gzip "$BACKUP_DIR/$FILENAME"

# Удалить бэкапы старше 30 дней
find "$BACKUP_DIR" -name "*.sql.gz" -mtime +30 -delete
```

**Cron задача:**
```bash
# Ежедневно в 2:00 ночи
0 2 * * * /path/to/backup.sh
```

#### Восстановление из backup

```bash
gunzip autotest_ui_20251205_020000.sql.gz
psql -U postgres -d autotest_ui < autotest_ui_20251205_020000.sql
```

### 2. Файлы (media)

**Backup скриншотов:**
```bash
#!/bin/bash
BACKUP_DIR="/backups/media"
DATE=$(date +"%Y%m%d")

tar -czf "$BACKUP_DIR/media_$DATE.tar.gz" /path/to/autotest_ui/media/

# Удалить бэкапы старше 14 дней
find "$BACKUP_DIR" -name "media_*.tar.gz" -mtime +14 -delete
```

### 3. Рекомендации

- ✅ **Частота бэкапов БД:** ежедневно (минимум)
- ✅ **Частота бэкапов файлов:** еженедельно
- ✅ **Хранение:** минимум 30 дней для БД, 14 дней для файлов
- ✅ **Тестирование восстановления:** ежемесячно
- ✅ **Offsite backup:** храните копии вне сервера (S3, другой сервер)
- ✅ **Шифрование бэкапов:** для критичных данных

---

## Интеграции и API

### 1. Webhook Security

#### GitHub Webhooks

**Проверка HMAC подписи:**

```python
import hmac
import hashlib

def verify_github_signature(request):
    signature = request.headers.get('X-Hub-Signature-256', '')
    secret = settings.GITHUB_WEBHOOK_SECRET
    
    expected = 'sha256=' + hmac.new(
        secret.encode(),
        request.body,
        hashlib.sha256
    ).hexdigest()
    
    if not hmac.compare_digest(signature, expected):
        raise PermissionDenied('Invalid signature')
```

**Настройка webhook:**
1. GitHub → Repository Settings → Webhooks
2. URL: `https://yourdomain.com/api/webhooks/github/`
3. Secret: `GITHUB_WEBHOOK_SECRET` (из .env)
4. Events: `push`, `pull_request`

#### GitLab Webhooks

**Проверка токена:**

```python
def verify_gitlab_token(request):
    token = request.headers.get('X-Gitlab-Token', '')
    expected = settings.GITLAB_WEBHOOK_TOKEN
    
    if not hmac.compare_digest(token, expected):
        raise PermissionDenied('Invalid token')
```

#### Jenkins Webhooks

**Проверка секрета:**

```python
def verify_jenkins_secret(request):
    auth_header = request.headers.get('Authorization', '')
    expected = f'Bearer {settings.JENKINS_WEBHOOK_SECRET}'
    
    if not hmac.compare_digest(auth_header, expected):
        raise PermissionDenied('Invalid secret')
```

### 2. Jira Integration

**Безопасное хранение credentials:**

```python
# settings.py
JIRA_URL = os.getenv('JIRA_URL', '')
JIRA_USERNAME = os.getenv('JIRA_USERNAME', '')
JIRA_API_TOKEN = os.getenv('JIRA_API_TOKEN', '')  # НЕ пароль!
```

**Использование API Token вместо пароля:**
1. Jira → Account Settings → Security → API Tokens
2. Create API Token
3. Сохранить в `.env`

**Преимущества API Token:**
- ✅ Можно отозвать без смены пароля
- ✅ Ограниченные права
- ✅ Аудит использования

### 3. REST API Security

**Аутентификация:**

```python
REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': [
        'rest_framework.authentication.SessionAuthentication',
        'rest_framework.authentication.BasicAuthentication',
    ],
    'DEFAULT_PERMISSION_CLASSES': [
        'rest_framework.permissions.IsAuthenticatedOrReadOnly',
    ],
}
```

**Rate Limiting (рекомендуется):**

```python
REST_FRAMEWORK = {
    'DEFAULT_THROTTLE_CLASSES': [
        'rest_framework.throttling.AnonRateThrottle',
        'rest_framework.throttling.UserRateThrottle',
    ],
    'DEFAULT_THROTTLE_RATES': {
        'anon': '100/hour',
        'user': '1000/hour',
    },
}
```

---

## Процедуры реагирования

### 1. Обнаружение инцидента

**Индикаторы компрометации:**
- 🚨 Множественные неудачные попытки входа
- 🚨 Доступ к несуществующим URL (сканирование)
- 🚨 Необычная активность пользователя
- 🚨 Изменения в учетных записях администраторов
- 🚨 Неожиданные изменения в БД

**Автоматические уведомления:**
```python
# settings.py
ADMINS = [
    ('Admin Name', 'admin@yourdomain.com'),
]

# Django отправит email при 500 ошибке (если DEBUG=False)
```

### 2. Реагирование на инцидент

**Шаг 1: Изоляция**
1. Заблокировать скомпрометированную учетную запись
2. Изменить пароли администраторов
3. Ротация SECRET_KEY (приведёт к выходу всех пользователей)

**Шаг 2: Анализ**
1. Проверить логи (`logs/security.log`, PostgreSQL logs)
2. Проверить изменения в БД (audit trail)
3. Проверить файлы на диске

**Шаг 3: Восстановление**
1. Восстановить из backup (если нужно)
2. Устранить уязвимость
3. Обновить зависимости

**Шаг 4: Документирование**
1. Создать incident report
2. Обновить процедуры безопасности
3. Провести post-mortem

### 3. Ротация секретов

**Django SECRET_KEY:**

```python
# 1. Сгенерировать новый ключ
from django.core.management.utils import get_random_secret_key
new_key = get_random_secret_key()

# 2. Обновить .env
DJANGO_SECRET_KEY=new-secret-key-here

# 3. Перезапустить приложение
# Все пользователи будут разлогинены
```

**Webhook секреты:**

```bash
# 1. Сгенерировать новый секрет
openssl rand -hex 32

# 2. Обновить в .env
GITHUB_WEBHOOK_SECRET=new-secret

# 3. Обновить на GitHub/GitLab
# 4. Перезапустить приложение
```

**Jira API Token:**

```
1. Отозвать старый токен в Jira
2. Создать новый API Token
3. Обновить в .env
4. Перезапустить приложение
```

---

## Соответствие стандартам

### 1. OWASP Top 10 (2021)

| Уязвимость | Статус | Меры защиты |
|------------|--------|-------------|
| **A01: Broken Access Control** | ✅ | Разделение прав, проверка владельца, `@login_required` |
| **A02: Cryptographic Failures** | ✅ | Argon2/PBKDF2, HTTPS, безопасное хранение секретов |
| **A03: Injection** | ✅ | Django ORM, параметризированные запросы, валидация |
| **A04: Insecure Design** | ✅ | Принцип least privilege, безопасная архитектура |
| **A05: Security Misconfiguration** | ✅ | Безопасные defaults, явные настройки, .env |
| **A06: Vulnerable Components** | ⚠️ | Регулярное обновление зависимостей (требуется) |
| **A07: Authentication Failures** | ✅ | Строгая валидация паролей, защита от brute-force |
| **A08: Software and Data Integrity** | ✅ | Webhook подписи, Git signed commits |
| **A09: Security Logging** | ✅ | Django logging, Flower monitoring |
| **A10: SSRF** | ✅ | Валидация URL, whitelist доменов |

### 2. NIST Cybersecurity Framework

| Функция | Категория | Статус | Реализация |
|---------|-----------|--------|------------|
| **Identify** | Asset Management | ✅ | Документирование компонентов |
| | Risk Assessment | ⚠️ | Рекомендуется формализовать |
| **Protect** | Access Control | ✅ | Аутентификация, авторизация |
| | Data Security | ✅ | Хеширование, каскадное удаление |
| | Awareness | ⚠️ | Обучение пользователей (требуется) |
| **Detect** | Anomalies | ⚠️ | Логирование (базовое) |
| | Continuous Monitoring | ✅ | Flower, PostgreSQL logs |
| **Respond** | Response Planning | ⚠️ | Процедуры документированы в этом файле |
| | Communications | ⚠️ | Email уведомления администраторам |
| **Recover** | Recovery Planning | ✅ | Backup/restore процедуры |
| | Improvements | ⚠️ | Post-mortem после инцидентов |

### 3. ISO/IEC 27001

**Реализованные контроли:**

- **A.9 Access Control:**
  - ✅ A.9.1.1 Access control policy
  - ✅ A.9.2.1 User registration and de-registration
  - ✅ A.9.2.2 User access provisioning
  - ✅ A.9.2.3 Management of privileged access rights
  - ✅ A.9.4.1 Information access restriction

- **A.10 Cryptography:**
  - ✅ A.10.1.1 Policy on the use of cryptographic controls
  - ✅ A.10.1.2 Key management (для webhook секретов)

- **A.12 Operations Security:**
  - ✅ A.12.4.1 Event logging
  - ⚠️ A.12.4.2 Protection of log information
  - ⚠️ A.12.4.3 Administrator and operator logs

- **A.14 System Acquisition:**
  - ✅ A.14.2.1 Secure development policy
  - ✅ A.14.2.5 Secure system engineering principles

### 4. PCI DSS (если применимо)

**Не применимо**, так как система не обрабатывает платежные данные.

---

## Контрольный список безопасности

### Перед развертыванием в Production

**Критичные настройки:**

- [ ] `DEBUG = False`
- [ ] `SECRET_KEY` - уникальный, длинный (50+ символов)
- [ ] `ALLOWED_HOSTS` - конкретные домены (не `*`)
- [ ] `SECURE_SSL_REDIRECT = True`
- [ ] `SECURE_HSTS_SECONDS = 31536000`
- [ ] `SESSION_COOKIE_SECURE = True`
- [ ] `CSRF_COOKIE_SECURE = True`

**База данных:**

- [ ] PostgreSQL пароль изменён
- [ ] Доступ к БД только с localhost/VPN
- [ ] Настроено резервное копирование
- [ ] Connection pooling включен

**Пароли:**

- [ ] Argon2 установлен (`pip install argon2-cffi`)
- [ ] PASSWORD_HASHERS в правильном порядке
- [ ] Валидаторы паролей настроены

**Файлы:**

- [ ] `.env` в `.gitignore`
- [ ] MEDIA_ROOT вне web root
- [ ] Автоудаление файлов работает

**Сеть:**

- [ ] HTTPS настроен (TLS 1.2+)
- [ ] CORS настроен правильно
- [ ] Firewall настроен

**Мониторинг:**

- [ ] Логирование настроено
- [ ] Email уведомления администраторам
- [ ] Flower защищён (Basic Auth + localhost)

**Бэкапы:**

- [ ] Автоматические бэкапы БД
- [ ] Бэкапы файлов
- [ ] Протестировано восстановление

---

## Обновление политики

**Версионирование:**
- Мажорные изменения: увеличение основной версии (1.0 → 2.0)
- Минорные изменения: увеличение минорной версии (1.0 → 1.1)
- Исправления: увеличение патч-версии (1.0.0 → 1.0.1)

**Регулярность пересмотра:**
- Минимум: **1 раз в год**
- При обнаружении инцидента: **немедленно**
- При изменении требований: **по мере необходимости**

**Ответственный:** Администратор системы / Lead Developer

---

## Контакты

**По вопросам безопасности:**
- Email: security@yourdomain.com
- Responsible Disclosure: следуйте процедуре responsible disclosure

**Сообщение об уязвимости:**
1. Отправьте описание на security@yourdomain.com
2. Не публикуйте уязвимость публично до исправления
3. Ожидайте ответ в течение 48 часов

---

**Документ утверждён:** 05.12.2025  
**Следующий пересмотр:** 05.12.2026  
**Версия:** 1.0
