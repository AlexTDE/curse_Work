# 🚀 Инструкция по запуску проекта

## Быстрый старт (5 минут)

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

**Важно:** Требуется Python 3.10+ (рекомендуется 3.12)

### Шаг 2: Настройка базы данных

**Для разработки (SQLite) - самый простой вариант:**

```bash
cd autotest_ui
python manage.py migrate
python manage.py createsuperuser
```

**Для продакшена (PostgreSQL):**

1. Установите PostgreSQL и создайте базу данных:
```sql
CREATE DATABASE autotest_ui;
CREATE USER autotest_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE autotest_ui TO autotest_user;
```

2. Создайте файл `.env` в корне проекта:
```env
DB_NAME=autotest_ui
DB_USER=autotest_user
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
```

3. Примените миграции:
```bash
cd autotest_ui
python manage.py migrate
python manage.py createsuperuser
```

### Шаг 3: Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Django
DJANGO_SECRET_KEY=dev-secret-key-change-me
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Режим выполнения задач
# 1 = синхронно (без Celery) - проще для начала
# 0 = через Celery (требует Redis)
TASKS_FORCE_SYNC=1

# Если используете Celery (TASKS_FORCE_SYNC=0)
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0

# YOLOv8
USE_YOLO_DETECTION=1
YOLO_CONF_THRESHOLD=0.25
```

### Шаг 4: Запуск проекта

#### Вариант A: Синхронный режим (рекомендуется для начала)

**Windows:**
```powershell
# Используйте готовый скрипт
.\start_server.bat

# Или вручную
cd autotest_ui
python manage.py runserver
```

**Linux/Mac:**
```bash
cd autotest_ui
python manage.py runserver
```

#### Вариант B: С Celery (для продакшена)

1. Запустите Redis:
```bash
docker run -d -p 6379:6379 redis:7-alpine
```

2. Установите в `.env`:
```env
TASKS_FORCE_SYNC=0
```

3. Запустите в двух терминалах:

**Терминал 1 (Django):**
```bash
# Windows
.\start_server.bat

# Linux/Mac
cd autotest_ui
python manage.py runserver
```

**Терминал 2 (Celery):**
```bash
# Windows
.\start_celery.bat

# Linux/Mac
cd autotest_ui
celery -A autotest_ui worker -l info
```

### Шаг 5: Откройте в браузере

- **Веб-интерфейс:** http://localhost:8000/
- **API:** http://localhost:8000/api/
- **Админка:** http://localhost:8000/admin/

Войдите с учетными данными суперпользователя, созданного на шаге 2.

---

## 📋 Что дальше?

### 1. Создайте тест-кейс

1. Перейдите на http://localhost:8000/testcases/
2. Нажмите "Создать тест-кейс"
3. Загрузите эталонный скриншот макета
4. Нажмите "Анализ" для обнаружения UI элементов

Система автоматически:
- Обнаружит элементы с помощью YOLOv8
- Классифицирует их (button, input, label, image, link)
- Извлечет текст с помощью OCR (если установлен Tesseract)

### 2. Создайте прогон

1. Перейдите на http://localhost:8000/runs/
2. Нажмите "Создать прогон"
3. Выберите тест-кейс и загрузите скриншот реального интерфейса
4. Нажмите "Сравнить"

Система автоматически:
- Сравнит с эталоном используя SSIM
- Определит различия
- Создаст дефекты при обнаружении проблем
- Вычислит метрики покрытия

### 3. Просмотрите результаты

- Метрики покрытия элементов
- Обнаруженные дефекты
- Визуализация различий
- Детальная информация по каждому элементу

---

## ⚙️ Дополнительные настройки

### OCR (опционально, но рекомендуется)

Для распознавания текста установите Tesseract OCR:

- **Windows:** https://github.com/UB-Mannheim/tesseract/wiki
- **Linux:** `sudo apt-get install tesseract-ocr`
- **Mac:** `brew install tesseract`

После установки добавьте Tesseract в PATH или укажите путь в настройках.

### YOLOv8

Модель уже включена в проект: `autotest_ui/testsystem/ml_models/yolov8s.pt`

Если модель не найдена, система автоматически переключится на эвристические методы детектирования.

---

## 🐛 Решение проблем

### Проблема: Сервер не запускается

**Ошибка:** `ModuleNotFoundError`
- **Решение:** Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`

**Ошибка:** `Port 8000 is already in use`
- **Решение:** Остановите другой процесс на порту 8000 или используйте другой порт: `python manage.py runserver 8001`

### Проблема: Задачи не выполняются

**Решение:** Установите `TASKS_FORCE_SYNC=1` в `.env` для синхронного режима (без Celery)

### Проблема: База данных не работает

**Ошибка:** `django.db.utils.OperationalError`
- **Решение:** 
  - Проверьте настройки в `.env`
  - Убедитесь, что PostgreSQL запущен (если используете)
  - Для SQLite убедитесь, что есть права на запись в папку `autotest_ui/`

### Проблема: YOLOv8 не работает

**Решение:** 
- Убедитесь, что `ultralytics` установлен: `pip install ultralytics`
- Проверьте наличие файла `autotest_ui/testsystem/ml_models/yolov8s.pt`

### Проблема: OCR не работает

**Решение:**
- Установите Tesseract OCR (см. выше)
- Добавьте Tesseract в PATH
- Система продолжит работать без OCR, но текст не будет извлекаться

---

## 📚 Дополнительная документация

- [README.md](README.md) - Полное описание проекта
- [YOLOV8_INTEGRATION.md](YOLOV8_INTEGRATION.md) - Интеграция YOLOv8
- [SECURITY_AND_DELETE_IMPROVEMENTS.md](SECURITY_AND_DELETE_IMPROVEMENTS.md) - Безопасность
- [USER_GUIDE.md](USER_GUIDE.md) - Руководство пользователя
- [CI_CD_COMPLETE_GUIDE.md](CI_CD_COMPLETE_GUIDE.md) - Интеграция с CI/CD

---

## ✅ Чеклист запуска

- [ ] Python 3.10+ установлен
- [ ] Зависимости установлены (`pip install -r requirements.txt`)
- [ ] База данных настроена (SQLite или PostgreSQL)
- [ ] Миграции применены (`python manage.py migrate`)
- [ ] Суперпользователь создан (`python manage.py createsuperuser`)
- [ ] Файл `.env` создан с настройками
- [ ] Сервер запущен (`python manage.py runserver`)
- [ ] (Опционально) Celery worker запущен
- [ ] (Опционально) Redis запущен
- [ ] (Опционально) Tesseract OCR установлен

---

**Готово!** Проект должен работать на http://localhost:8000/
