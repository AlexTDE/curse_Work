# Интеграция с Jira - Трекер задач

## 🤔 Что такое Jira?

**Jira** — это система управления проектами и отслеживания задач (issue tracker), разработанная Atlassian. Используется для планирования, отслеживания и управления задачами в команде разработки.

---

## 🔧 Как работает интеграция в проекте

### Архитектура

```
┌──────────────┐
│   Система    │
│  тестирования│
└──────┬───────┘
       │
       │ Обнаружен дефект
       ▼
┌──────────────┐
│   Defect     │
│   (модель)   │
└──────┬───────┘
       │
       │ sync_defect_to_jira()
       ▼
┌──────────────┐         ┌──────────────┐
│ Jira Client  │ ──────> │   Jira API   │
│  (jira.py)   │         │   (Cloud/    │
└──────────────┘         │   Server)    │
                         └──────────────┘
                                │
                                │ Создает задачу
                                ▼
                         ┌──────────────┐
                         │  Jira Issue  │
                         │  (TEST-123)  │
                         └──────────────┘
```

### Процесс работы

1. **Обнаружение дефекта:**
   - Система сравнивает эталонный и актуальный UI
   - Если различия превышают порог, создается `Defect`

2. **Автоматическое создание задачи:**
   - При создании дефекта автоматически вызывается `sync_defect_to_jira()`
   - Создается задача в Jira с информацией о дефекте

3. **Синхронизация:**
   - Ключ задачи (например, `TEST-123`) сохраняется в `Defect.metadata['jira_issue_key']`
   - Также сохраняется в `Run.task_tracker_issue`

---

## 📋 Что создается в Jira

### Информация в задаче

**Summary (Заголовок):**
```
UI Defect: Название тест-кейса - тип элемента
```

**Description (Описание):**
```
Описание дефекта

**Test Case:** Название тест-кейса (ID: 1)
**Run ID:** 5
**Severity:** major

**Coverage:** 85.50%
**Diff Score:** 0.8234

**UI Element:** button (ID: 12)

**Screenshot:** /media/defects/screenshot.png
```

**Тип задачи:**
- `Bug` — для critical и major дефектов
- `Task` — для minor дефектов

---

## ⚙️ Настройка

### Шаг 1: Получите API токен

1. Зайдите в Jira: https://id.atlassian.com/manage-profile/security/api-tokens
2. Нажмите "Create API token"
3. Скопируйте токен

### Шаг 2: Настройте переменные окружения

Создайте или обновите файл `.env`:

```env
# Jira настройки
JIRA_URL=https://your-company.atlassian.net
JIRA_USERNAME=your-email@example.com
JIRA_API_TOKEN=your-api-token-here
JIRA_PROJECT_KEY=TEST
```

**Где взять значения:**
- `JIRA_URL`: URL вашего Jira (например, `https://company.atlassian.net`)
- `JIRA_USERNAME`: Ваш email в Jira
- `JIRA_API_TOKEN`: Токен, созданный на шаге 1
- `JIRA_PROJECT_KEY`: Ключ проекта в Jira (например, `TEST`, `PROJ`, `DEV`)

### Шаг 3: Проверка работы

1. Создайте тест-кейс и прогон
2. Запустите сравнение
3. Если обнаружен дефект, автоматически создастся задача в Jira
4. Ключ задачи появится в поле `task_tracker_issue` прогона

---

## 🔄 Как это работает в коде

### Автоматическое создание задачи

**Файл:** `autotest_ui/testsystem/tasks.py`

```python
# При обнаружении дефекта
if ssim_score < threshold or mismatch_ratio > diff_threshold:
    defect = Defect.objects.create(
        testcase=testcase,
        run=run,
        description='UI deviation exceeds threshold',
        severity='major',
        ...
    )
    
    # Автоматически создаем задачу в Jira
    try:
        from .jira_integration import sync_defect_to_jira
        jira_issue_key = sync_defect_to_jira(defect)
        if jira_issue_key:
            logger.info(f"Created Jira issue {jira_issue_key}")
    except Exception as e:
        logger.warning(f"Failed to create Jira issue: {e}")
```

### Функции интеграции

**Файл:** `autotest_ui/testsystem/jira_integration.py`

#### `get_jira_client()`
- Создает клиент Jira с настройками из `.env`
- Возвращает `None`, если настройки не указаны

#### `create_jira_issue_from_defect(defect)`
- Создает задачу в Jira на основе дефекта
- Возвращает ключ задачи (например, `TEST-123`)

#### `sync_defect_to_jira(defect)`
- Проверяет, есть ли уже задача для дефекта
- Если нет — создает новую
- Сохраняет ключ задачи в `defect.metadata` и `run.task_tracker_issue`

#### `update_jira_issue_status(issue_key, status)`
- Обновляет статус задачи в Jira
- Статусы: 'To Do', 'In Progress', 'Done', 'Closed'

#### `get_jira_issue_url(issue_key)`
- Возвращает URL задачи в Jira
- Например: `https://company.atlassian.net/browse/TEST-123`

---

## 📊 Пример использования

### Автоматическое создание (уже работает)

```python
# В tasks.py при обнаружении дефекта
defect = Defect.objects.create(...)
jira_issue_key = sync_defect_to_jira(defect)
# Результат: "TEST-123"
```

### Ручное создание задачи

```python
from testsystem.jira_integration import create_jira_issue_from_defect

defect = Defect.objects.get(id=1)
issue_key = create_jira_issue_from_defect(defect)
print(f"Created issue: {issue_key}")
```

### Обновление статуса

```python
from testsystem.jira_integration import update_jira_issue_status

update_jira_issue_status('TEST-123', 'In Progress')
```

### Получение URL

```python
from testsystem.jira_integration import get_jira_issue_url

url = get_jira_issue_url('TEST-123')
# Результат: "https://company.atlassian.net/browse/TEST-123"
```

---

## 🔍 Где хранится информация

### В базе данных

**Модель `Defect`:**
- `metadata['jira_issue_key']` — ключ задачи в Jira

**Модель `Run`:**
- `task_tracker_issue` — ключ задачи в Jira (строка)

### Пример

```python
defect = Defect.objects.get(id=1)
print(defect.metadata['jira_issue_key'])  # "TEST-123"

run = Run.objects.get(id=5)
print(run.task_tracker_issue)  # "TEST-123"
```

---

## ⚠️ Важные замечания

1. **Интеграция опциональна:**
   - Если Jira не настроена, система продолжит работать
   - Дефекты будут создаваться, но задачи в Jira не будут создаваться

2. **Ошибки не прерывают работу:**
   - Если не удалось создать задачу в Jira, ошибка логируется
   - Система продолжает работать нормально

3. **Один дефект = одна задача:**
   - Для каждого дефекта создается отдельная задача
   - Если задача уже существует, новая не создается

4. **Тип задачи:**
   - Critical/Major дефекты → `Bug`
   - Minor дефекты → `Task`

---

## 🐛 Решение проблем

### Проблема: Задачи не создаются

**Проверьте:**
1. Настройки в `.env` правильные
2. API токен действителен
3. Проект `JIRA_PROJECT_KEY` существует в Jira
4. У пользователя есть права на создание задач в проекте

**Логи:**
```python
# Проверьте логи Django
# Должны быть сообщения:
# "Created Jira issue TEST-123" - успех
# "Failed to create Jira issue" - ошибка
```

### Проблема: Ошибка подключения

**Решение:**
- Проверьте `JIRA_URL` (должен быть полный URL с `https://`)
- Проверьте `JIRA_USERNAME` (должен быть email)
- Проверьте `JIRA_API_TOKEN` (должен быть действительным)

---

## 📚 Дополнительная информация

### API Jira

Интеграция использует библиотеку `jira-python`:
- Документация: https://jira.readthedocs.io/
- Установка: `pip install jira`

### Типы задач в Jira

По умолчанию используются:
- `Bug` — для ошибок
- `Task` — для задач

Можно изменить в `jira_integration.py` в функции `create_jira_issue_from_defect()`.

---

**Итого:** Интеграция с Jira автоматически создает задачи при обнаружении дефектов. Настройка простая — нужно только указать учетные данные в `.env`. Если Jira не настроена, система работает без неё.

