# Улучшения безопасности и удаления

## ✅ Что было сделано

### 1. Усиление безопасности API

#### Изменения в `autotest_ui/testsystem/views.py`:

**TestCaseViewSet:**
- ✅ Все операции записи (создание, изменение, удаление) теперь требуют авторизации
- ✅ Операции чтения (list, retrieve, elements) доступны неавторизованным пользователям (для совместимости с CI/CD)
- ✅ Добавлена проверка прав доступа при удалении: только владелец или суперпользователь может удалить тест-кейс
- ✅ Добавлена проверка прав доступа при анализе: только владелец или суперпользователь может запускать анализ

**RunViewSet:**
- ✅ Все операции записи (создание, изменение, удаление) теперь требуют авторизации
- ✅ Операции чтения (list, retrieve, ci_status, ci_status_detail) доступны неавторизованным пользователям (для совместимости с CI/CD)
- ✅ Добавлена проверка прав доступа при удалении: только владелец прогона, владелец тест-кейса или суперпользователь может удалить прогон
- ✅ Добавлена проверка прав доступа при сравнении: только владелец прогона, владелец тест-кейса или суперпользователь может запускать сравнение

**UIElementViewSet, DefectViewSet, CoverageMetricViewSet:**
- ✅ Изменено с `AllowAny` на `IsAuthenticatedOrReadOnly` для безопасности

### 2. Улучшение функций удаления в веб-интерфейсе

#### Изменения в `autotest_ui/testsystem/views_web.py`:

**delete_testcase:**
- ✅ Добавлена проверка прав доступа: только владелец или суперпользователь может удалить тест-кейс
- ✅ Добавлено удаление связанных файлов (скриншотов) при удалении тест-кейса
- ✅ Улучшена обработка ошибок

**delete_run:**
- ✅ Добавлена проверка прав доступа: только владелец прогона, владелец тест-кейса или суперпользователь может удалить прогон
- ✅ Добавлено удаление связанных файлов (скриншотов) при удалении прогона
- ✅ Улучшена обработка ошибок

**analyze_testcase:**
- ✅ Добавлена проверка прав доступа: только владелец или суперпользователь может запускать анализ

**compare_run:**
- ✅ Добавлена проверка прав доступа: только владелец прогона, владелец тест-кейса или суперпользователь может запускать сравнение

### 3. Настройки безопасности

#### Изменения в `autotest_ui/autotest_ui/settings.py`:

- ✅ Добавлены классы аутентификации по умолчанию:
  - `SessionAuthentication` - для веб-интерфейса
  - `BasicAuthentication` - для API запросов
- ✅ Права доступа по умолчанию: `IsAuthenticatedOrReadOnly` (чтение доступно всем, запись только авторизованным)

---

## 🔒 Правила доступа

### Тест-кейсы (TestCase)

**Чтение:**
- ✅ Все пользователи (включая неавторизованных) могут просматривать тест-кейсы

**Создание:**
- ✅ Только авторизованные пользователи могут создавать тест-кейсы
- ✅ Автоматически устанавливается `created_by = request.user`

**Изменение:**
- ✅ Только авторизованные пользователи могут изменять тест-кейсы

**Удаление:**
- ✅ Только владелец тест-кейса (`created_by`) или суперпользователь может удалить тест-кейс

**Анализ:**
- ✅ Только владелец тест-кейса или суперпользователь может запускать анализ

### Прогоны (Run)

**Чтение:**
- ✅ Все пользователи (включая неавторизованных) могут просматривать прогоны

**Создание:**
- ✅ Только авторизованные пользователи могут создавать прогоны
- ✅ Автоматически устанавливается `started_by = request.user`

**Изменение:**
- ✅ Только авторизованные пользователи могут изменять прогоны

**Удаление:**
- ✅ Владелец прогона (`started_by`) может удалить свой прогон
- ✅ Владелец тест-кейса может удалить прогоны своего тест-кейса
- ✅ Суперпользователь может удалять все прогоны

**Сравнение:**
- ✅ Владелец прогона может запускать сравнение своего прогона
- ✅ Владелец тест-кейса может запускать сравнение прогонов своего тест-кейса
- ✅ Суперпользователь может запускать сравнение всех прогонов

---

## 🛡️ Безопасность

### API Endpoints

- ✅ Все операции записи (POST, PUT, PATCH, DELETE) требуют авторизации
- ✅ Операции чтения (GET) доступны неавторизованным пользователям (для совместимости с CI/CD)
- ✅ CI/CD webhooks используют `AllowAny` (но проверяют подпись)

### Веб-интерфейс

- ✅ Все страницы защищены декоратором `@login_required`
- ✅ Все операции удаления требуют подтверждения (через JavaScript `confirm()`)
- ✅ Проверка прав доступа выполняется на сервере (не только на клиенте)

### Файлы

- ✅ При удалении тест-кейсов и прогонов автоматически удаляются связанные файлы (скриншоты)
- ✅ Ошибки удаления файлов логируются, но не прерывают процесс удаления

---

## 📝 Примеры использования

### Удаление тест-кейса через API

```python
import requests

# Авторизация
session = requests.Session()
session.auth = ('username', 'password')

# Удаление тест-кейса (только если вы владелец)
response = session.delete('http://localhost:8000/api/testcases/1/')
# 204 No Content - успешно удалено
# 403 Forbidden - нет прав доступа
# 404 Not Found - тест-кейс не найден
```

### Удаление прогона через API

```python
# Удаление прогона (только если вы владелец прогона или тест-кейса)
response = session.delete('http://localhost:8000/api/runs/1/')
# 204 No Content - успешно удалено
# 403 Forbidden - нет прав доступа
# 404 Not Found - прогон не найден
```

### Удаление через веб-интерфейс

1. Перейдите на страницу тест-кейса или прогона
2. Нажмите кнопку "🗑️ Удалить"
3. Подтвердите удаление в диалоговом окне
4. Если у вас нет прав доступа, вы увидите сообщение об ошибке

---

## ⚠️ Важные замечания

1. **CI/CD интеграция:** Webhooks для CI/CD используют `AllowAny`, но проверяют подпись для безопасности
2. **Суперпользователи:** Суперпользователи Django могут выполнять все операции без ограничений
3. **Каскадное удаление:** При удалении тест-кейса автоматически удаляются все связанные элементы UI и прогоны (через CASCADE в модели)
4. **Файлы:** Скриншоты удаляются автоматически, но ошибки удаления файлов не прерывают процесс

---

## 🔍 Проверка безопасности

Для проверки безопасности можно использовать:

```python
# Проверка прав доступа
from django.contrib.auth.models import User
from testsystem.models import TestCase, Run

user = User.objects.get(username='testuser')
testcase = TestCase.objects.get(id=1)

# Проверка владельца
if testcase.created_by == user:
    print("Пользователь может удалить этот тест-кейс")
else:
    print("Пользователь НЕ может удалить этот тест-кейс")
```

---

**Дата изменений:** 2025-11-26  
**Статус:** ✅ Готово к использованию



